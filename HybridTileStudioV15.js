/* Hybrid Tile Studio v15 — Creator Console */
(() => {
    "use strict";
    const api = window.HybridTileStudio;
    const v13 = window.HybridTileStudioV13;
    const v14 = window.HybridTileStudioV14;
    if (!api || !v13 || !v14 || !/^(15|16|17|18)\./.test(String(api.version))) return;

    const $ = id => document.getElementById(id);
    const clone = value => value === undefined ? undefined : JSON.parse(JSON.stringify(value));
    const list = value => Array.isArray(value) ? value : value === undefined || value === null || value === "" ? [] : [value];
    const integer = (value, fallback = 0) => Number.isFinite(Number(value)) ? Math.trunc(Number(value)) : fallback;
    const number = (value, fallback = 0) => Number.isFinite(Number(value)) ? Number(value) : fallback;
    const clamp = (value, min, max) => Math.max(min, Math.min(max, value));
    const uid = prefix => `${prefix}-${Date.now().toString(36)}-${Math.random().toString(36).slice(2, 8)}`;
    const safeId = value => String(value || "").trim().toLowerCase().replace(/[^a-z0-9._:-]+/g, "-").replace(/^-+|-+$/g, "").slice(0, 160);
    const escapeHtml = value => String(value ?? "").replace(/[&<>"']/g, character => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" })[character]);
    const parseJson = (text, fallback = null) => { try { return JSON.parse(String(text || "")); } catch (_) { return clone(fallback); } };
    const parseValue = value => { const text = String(value ?? "").trim(); if (text === "true") return true; if (text === "false") return false; if (text === "null") return null; if (text !== "" && Number.isFinite(Number(text))) return Number(text); return value; };
    const delay = ms => new Promise(resolve => setTimeout(resolve, ms));
    const safe = callback => async event => { event?.preventDefault?.(); try { await callback(event); } catch (error) { console.error(error); notify(error?.message || String(error), "error"); api.status(error?.message || String(error), true); } };

    const VIEWS = [
        ["home", "Home", "⌂"], ["events", "Events", "◇"], ["tilesets", "Tilesets", "▦"], ["live", "Live", "●"],
        ["tests", "Tests", "✓"], ["search", "Search", "⌕"], ["intelligence", "Intelligence", "✦"], ["performance", "Performance", "⌁"],
        ["extensions", "Extensions", "⬡"], ["team", "Team", "◎"], ["ship", "Ship", "↑"]
    ];
    const EVENT_COMMANDS = [
        [101,"Show Text","Message"],[102,"Show Choices","Message"],[103,"Input Number","Message"],[104,"Select Item","Message"],[105,"Show Scrolling Text","Message"],[108,"Comment","Flow"],
        [111,"Conditional Branch","Flow"],[112,"Loop","Flow"],[113,"Break Loop","Flow"],[115,"Exit Event Processing","Flow"],[117,"Common Event","Flow"],[118,"Label","Flow"],[119,"Jump to Label","Flow"],
        [121,"Control Switches","State"],[122,"Control Variables","State"],[123,"Control Self Switch","State"],[124,"Control Timer","State"],[125,"Change Gold","Party"],[126,"Change Items","Party"],[127,"Change Weapons","Party"],[128,"Change Armors","Party"],[129,"Change Party Member","Party"],
        [132,"Change Battle BGM","System"],[133,"Change Victory ME","System"],[134,"Change Save Access","System"],[135,"Change Menu Access","System"],[136,"Change Encounter","System"],[137,"Change Formation Access","System"],[138,"Change Window Color","System"],
        [201,"Transfer Player","Movement"],[202,"Set Vehicle Location","Movement"],[203,"Set Event Location","Movement"],[204,"Scroll Map","Movement"],[205,"Set Movement Route","Movement"],[206,"Get On/Off Vehicle","Movement"],
        [211,"Change Transparency","Character"],[212,"Show Animation","Character"],[213,"Show Balloon Icon","Character"],[214,"Erase Event","Character"],[216,"Change Player Followers","Character"],[217,"Gather Followers","Character"],
        [221,"Fadeout Screen","Screen"],[222,"Fadein Screen","Screen"],[223,"Tint Screen","Screen"],[224,"Flash Screen","Screen"],[225,"Shake Screen","Screen"],[230,"Wait","Flow"],
        [231,"Show Picture","Picture"],[232,"Move Picture","Picture"],[233,"Rotate Picture","Picture"],[234,"Tint Picture","Picture"],[235,"Erase Picture","Picture"],[236,"Set Weather Effect","Screen"],
        [241,"Play BGM","Audio"],[242,"Fadeout BGM","Audio"],[243,"Save BGM","Audio"],[244,"Resume BGM","Audio"],[245,"Play BGS","Audio"],[246,"Fadeout BGS","Audio"],[249,"Play ME","Audio"],[250,"Play SE","Audio"],[251,"Stop SE","Audio"],
        [261,"Play Movie","Screen"],[281,"Change Map Name Display","Map"],[282,"Change Tileset","Map"],[283,"Change Battle Background","Map"],[284,"Change Parallax","Map"],[285,"Get Location Info","Map"],
        [301,"Battle Processing","Scene"],[302,"Shop Processing","Scene"],[303,"Name Input Processing","Scene"],
        [311,"Change HP","Actor"],[312,"Change MP","Actor"],[313,"Change State","Actor"],[314,"Recover All","Actor"],[315,"Change EXP","Actor"],[316,"Change Level","Actor"],[317,"Change Parameter","Actor"],[318,"Change Skill","Actor"],[319,"Change Equipment","Actor"],[320,"Change Name","Actor"],[321,"Change Class","Actor"],[322,"Change Actor Images","Actor"],[323,"Change Vehicle Image","Actor"],[324,"Change Nickname","Actor"],[325,"Change Profile","Actor"],[326,"Change TP","Actor"],
        [331,"Change Enemy HP","Battle"],[332,"Change Enemy MP","Battle"],[333,"Change Enemy State","Battle"],[334,"Enemy Recover All","Battle"],[335,"Enemy Appear","Battle"],[336,"Enemy Transform","Battle"],[337,"Show Battle Animation","Battle"],[339,"Force Action","Battle"],[340,"Abort Battle","Battle"],[342,"Change Enemy TP","Battle"],
        [351,"Open Menu Screen","Scene"],[352,"Open Save Screen","Scene"],[353,"Game Over","Scene"],[354,"Return to Title Screen","Scene"],[355,"Script","Advanced"],[356,"Plugin Command (MV)","Advanced"],[357,"Plugin Command (MZ)","Advanced"]
    ];
    const COMMAND_NAMES = new Map([...EVENT_COMMANDS.map(([code, name]) => [code, name]), [0,"End"],[401,"Text continuation"],[402,"Choice branch"],[403,"Choice cancel"],[404,"End choices"],[405,"Scroll continuation"],[408,"Comment continuation"],[411,"Else"],[412,"End branch"],[413,"Repeat above"],[601,"Battle win"],[602,"Battle escape"],[603,"Battle lose"],[604,"End battle result"],[605,"Shop goods"],[655,"Script continuation"],[657,"Plugin parameter"]]);
    const PARAMETER_LABELS = {
        101:["Face image","Face index","Window background","Window position","Speaker name"],102:["Choices","Cancel behavior","Default choice","Position","Background"],103:["Variable ID","Digits"],104:["Variable ID","Item type"],105:["Speed","No fast-forward"],108:["Comment"],
        111:["Condition type","Operand A","Operand B","Operand C","Operand D","Operand E"],117:["Common event ID"],118:["Label"],119:["Label"],121:["First switch","Last switch","Value"],122:["First variable","Last variable","Operation","Operand type","Operand A","Operand B"],123:["Self switch","Value"],124:["Operation","Seconds"],
        125:["Operation","Operand type","Amount"],126:["Item ID","Operation","Operand type","Amount","Include equipment"],127:["Weapon ID","Operation","Operand type","Amount","Include equipment"],128:["Armor ID","Operation","Operand type","Amount","Include equipment"],129:["Actor ID","Operation","Initialize"],
        201:["Location mode","Map ID","X","Y","Direction","Fade"],202:["Vehicle","Location mode","Map ID","X","Y"],203:["Event","Location mode","X / event","Y","Direction"],204:["Direction","Distance","Speed","Wait"],205:["Character","Movement route"],
        211:["Transparency"],212:["Character","Animation ID","Wait"],213:["Character","Balloon ID","Wait"],216:["Followers"],223:["Tone","Duration","Wait"],224:["Color","Duration","Wait"],225:["Power","Speed","Duration","Wait"],230:["Frames"],
        231:["Picture ID","Name","Origin","Position mode","X","Y","Scale X","Scale Y","Opacity","Blend mode"],232:["Picture ID","Origin","Position mode","X","Y","Scale X","Scale Y","Opacity","Blend mode","Duration","Wait"],233:["Picture ID","Speed"],234:["Picture ID","Tone","Duration","Wait"],235:["Picture ID"],236:["Weather","Power","Duration","Wait"],
        241:["Audio"],242:["Seconds"],245:["Audio"],246:["Seconds"],249:["Audio"],250:["Audio"],261:["Movie"],281:["Display map name"],282:["Tileset ID"],283:["Battleback 1","Battleback 2"],284:["Parallax name","Loop X","Loop Y","Scroll X","Scroll Y"],285:["Variable ID","Information type","Location mode","Map / X","X / Y","Y"],
        301:["Troop mode","Troop ID / variable","Can escape","Can lose"],302:["Goods","Purchase only"],303:["Actor ID","Max characters"],355:["Script"],356:["Command"],357:["Plugin","Command","Display name","Arguments"]
    };
    const ROUTE_COMMANDS = [[1,"Move down"],[2,"Move left"],[3,"Move right"],[4,"Move up"],[5,"Move lower-left"],[6,"Move lower-right"],[7,"Move upper-left"],[8,"Move upper-right"],[9,"Move random"],[10,"Move toward player"],[11,"Move away from player"],[12,"Move forward"],[13,"Move backward"],[14,"Jump"],[15,"Wait"],[16,"Turn down"],[17,"Turn left"],[18,"Turn right"],[19,"Turn up"],[20,"Turn right 90°"],[21,"Turn left 90°"],[22,"Turn 180°"],[23,"Turn random"],[29,"Change speed"],[30,"Change frequency"],[31,"Walking animation ON"],[32,"Walking animation OFF"],[37,"Through ON"],[38,"Through OFF"],[41,"Change image"],[44,"Play SE"],[45,"Script"],[0,"End route"]];

    const defaults = {
        view:"home", mode:"guided", theme:"cloud", scale:1, sound:true, reducedMotion:false, selectedMapId:0, selectedEventId:0, selectedPage:0, selectedCommand:0,
        selectedTilesetId:1, selectedTileId:0, tileBank:"B", tilePaintMode:"inspect", live:{connected:false,recording:false,watchedSwitches:"1,2,3",watchedVariables:"1,2,3",sequence:0,commands:[],logs:[]},
        recordings:[], scenarios:[], scenarioRuns:[], goldenMaps:{}, searchQuery:"", searchResults:[], bookmarks:[], favorites:[], semanticProfiles:{}, extensionSecurity:{}, publishers:{}, reviewBundles:[], importedBundles:[], releaseReports:[], releaseFingerprints:[], recoveryPoints:[], compatibilityProfiles:[], notifications:[], recentCommands:[], onboarding:{project:false,event:false,tileset:false,live:false,test:false,search:false,ship:false}
    };
    const persisted = api.getExtensionData("creator-console-v15", {});
    const state = Object.assign(clone(defaults), persisted); state.live=Object.assign(clone(defaults.live),state.live||{});state.onboarding=Object.assign(clone(defaults.onboarding),state.onboarding||{});
    for(const key of ["recordings","scenarios","scenarioRuns","searchResults","bookmarks","favorites","reviewBundles","importedBundles","releaseReports","releaseFingerprints","recoveryPoints","compatibilityProfiles","notifications","recentCommands"])state[key]=list(state[key]);
    state.goldenMaps ||= {}; state.semanticProfiles ||= {}; state.extensionSecurity ||= {}; state.publishers ||= {};
    let persistTimer=0,pollTimer=0,eventDraft=null,eventDraftDirty=false,eventDraftMapId=0,eventDraftEventId=0,currentMap=null,tilesets=null,tilesetsDirty=false,pluginCommands=[],intelligence=null,performanceReport=null,searchPlan=null,lastGamepad={},sheetUrls=[],history=[],redoHistory=[],audioContext=null,cleanupArmed=false;
    const persist=()=>{clearTimeout(persistTimer);persistTimer=setTimeout(()=>api.setExtensionData("creator-console-v15",state).catch(()=>{}),100);};

    function inject(){
        document.documentElement.classList.add("v15-creator-console");
        if($("v14Launch"))$("v14Launch").hidden=true;
        const logo=$("v12HomeButton");if(logo){const version=logo.querySelector("small");if(version)version.textContent="v15";}
        const utilities=document.querySelector("#v12Nav .v12-utilities");if(utilities&&!$("v15Launch"))utilities.insertAdjacentHTML("afterbegin",'<button id="v15Launch" class="v15-launch" type="button"><i>✦</i><span><strong>CREATOR CONSOLE</strong><small>Production Suite v15</small></span><b>OPEN</b></button>');
        document.body.insertAdjacentHTML("beforeend",shell());wire();applyPreferences();updateBadge();
        document.addEventListener("HybridTileStudio:project-opened",safe(async()=>{state.selectedMapId=api.projectInfo().maps[0]?.id||0;eventDraft=null;eventDraftDirty=false;tilesets=null;tilesetsDirty=false;performanceReport=null;intelligence=null;state.onboarding.project=true;persist();await inspectCrashJournal();if($("v15Studio")?.open)render(state.view);}));
        document.addEventListener("HybridTileStudio:v14-associated",event=>importAssociated(event.detail?.value,event.detail?.name));
        document.addEventListener("HybridTileStudio:extension-manifest-opened",event=>importAssociated(event.detail?.value,event.detail?.name));
        document.dispatchEvent(new CustomEvent("HybridTileStudio:v15-ready",{detail:{version:"15.0.0",views:VIEWS.length,eventCommands:EVENT_COMMANDS.length}}));
    }
    function shell(){return`<dialog id="v15Studio" class="v15-studio"><div class="v15-app">
      <header class="v15-top"><button id="v15Back" aria-label="Close">←</button><div class="v15-brand"><i>H</i><span><strong>Hybrid Tile Studio</strong><small>CREATOR CONSOLE · v15</small></span></div><div class="v15-project"><strong id="v15Project">No project</strong><small id="v15ProjectMeta">Open a project to begin</small></div><button id="v15Undo" title="Undo">↶</button><button id="v15Redo" title="Redo">↷</button><button id="v15Mode">Guided</button><button id="v15Commands">⌕ <span>Commands</span><kbd>Ctrl K</kbd></button><button id="v15Notices">♢<b id="v15NoticeCount">0</b></button></header>
      <aside class="v15-rail">${VIEWS.map(([id,label,icon])=>`<button data-v15-view="${id}"><i>${icon}</i><span>${label}</span></button>`).join("")}<div></div><button id="v15Help"><i>?</i><span>Guide</span></button></aside>
      <main id="v15Main" class="v15-main" aria-live="polite"></main><footer class="v15-footer"><span id="v15Status"><i></i> Ready</span><div><kbd>A</kbd> Select <kbd>B</kbd> Back <kbd>X</kbd> Commands <kbd>Y</kbd> Guide <kbd>LB</kbd><kbd>RB</kbd> Areas</div></footer>
      </div></dialog>
      <dialog id="v15Palette" class="v15-dialog"><form method="dialog"><header><i>⌕</i><input id="v15PaletteQuery" type="search" placeholder="Find any tool, map, event, or action…"><kbd>ESC</kbd></header><main id="v15PaletteResults"></main></form></dialog>
      <dialog id="v15Sheet" class="v15-sheet"><form method="dialog"><header><div><small id="v15SheetKicker">CREATOR GUIDE</small><h2 id="v15SheetTitle">Create with confidence</h2></div><button value="close">×</button></header><main id="v15SheetBody"></main><footer><button value="close" class="v15-primary">Done</button></footer></form></dialog>
      <input id="v15Import" type="file" hidden accept=".htgrecording,.htgscenario,.htgjourney,.htgtestmanifest,.htgreview,.htgmerge,.htgfingerprint,.htgmanifest,.htgcompat,.htgrecovery,.htgextension,.json"><div id="v15Toast" class="v15-toast" role="status"></div>`;}
    function wire(){
        $("v15Launch").onclick=()=>open();$("v15Back").onclick=close;$("v15Studio").addEventListener("cancel",event=>{event.preventDefault();close();});
        $("v15Studio").onclick=event=>{const nav=event.target.closest("[data-v15-view]");if(nav)switchView(nav.dataset.v15View);const action=event.target.closest("[data-v15-action]");if(action)safe(()=>runAction(action.dataset.v15Action,action))(event);};
        $("v15Mode").onclick=()=>{state.mode=state.mode==="guided"?"expert":"guided";persist();applyPreferences();render(state.view);};
        $("v15Undo").onclick=()=>undoUniversal();$("v15Redo").onclick=()=>redoUniversal();$("v15Commands").onclick=openPalette;$("v15PaletteQuery").oninput=renderPalette;$("v15Palette").onclick=event=>{const item=event.target.closest("[data-v15-command]");if(item){$("v15Palette").close();executeCommand(item.dataset.v15Command);}};
        $("v15Notices").onclick=openNotices;$("v15Help").onclick=openGuide;$("v15Import").onchange=event=>importFiles([...event.target.files]);
        window.addEventListener("keydown",handleKeys,{capture:true});if(!/HappyDOM/i.test(navigator.userAgent||""))requestAnimationFrame(pollGamepad);
    }
    function open(view=state.view){for(const id of ["v13Studio","v14Studio"])if($(id)?.open)(id==="v14Studio"?v14:v13).close();const dialog=$("v15Studio");if(!dialog.open)dialog.showModal();switchView(VIEWS.some(item=>item[0]===view)?view:"home");}
    function close(){clearInterval(pollTimer);pollTimer=0;$("v15Studio").close();persist();}
    function switchView(view){state.view=view;persist();render(view);tone("move");}
    function applyPreferences(){const dialog=$("v15Studio");if(dialog){dialog.dataset.mode=state.mode;dialog.dataset.theme=state.theme;dialog.style.setProperty("--v15-scale",String(state.scale));}document.documentElement.classList.toggle("v15-reduced-motion",!!state.reducedMotion);if($("v15Mode"))$("v15Mode").textContent=state.mode==="expert"?"Expert":"Guided";}
    function updateChrome(){const info=api.projectInfo();$("v15Project").textContent=info.open?info.name:api.state().activeMapId?"Loose map workspace":"No project";$("v15ProjectMeta").textContent=info.open?`${info.maps.length} maps · ${state.live.connected?"Live connected":"Offline authoring"}`:api.state().activeMapId?`Map ${api.state().activeMapId}`:"Choose a project folder";document.querySelectorAll("[data-v15-view]").forEach(button=>button.classList.toggle("active",button.dataset.v15View===state.view));$("v15Undo").disabled=!history.length;$("v15Redo").disabled=!redoHistory.length;}
    async function render(view){updateChrome();$("v15Main").innerHTML='<section class="v15-loading"><i></i><strong>Preparing Creator Console…</strong></section>';if(view!=="home"&&!api.projectInfo().open&&!api.state().activeMapId){renderEmpty();return;}const renderer={home:renderHome,events:renderEvents,tilesets:renderTilesets,live:renderLive,tests:renderTests,search:renderSearch,intelligence:renderIntelligence,performance:renderPerformance,extensions:renderExtensions,team:renderTeam,ship:renderShip}[view]||renderHome;await renderer();buildCommands();updateChrome();}
    function renderEmpty(){$("v15Main").innerHTML=`<section class="v15-empty"><div class="v15-empty-orb">▦</div><small>CREATOR CONSOLE</small><h1>Open your RPG Maker project</h1><p>Visual authoring, live playtests, universal recovery, automated testing, and release engineering stay together.</p><div><button data-v15-action="open-project" class="v15-primary">Choose Project Folder</button><button data-v15-action="practice">Open Practice Workspace</button></div></section>`;}
    function pageHeader(kicker,title,copy,actions=""){return`<header class="v15-page-head"><div><small>${escapeHtml(kicker)}</small><h1>${escapeHtml(title)}</h1><p>${escapeHtml(copy)}</p></div><aside>${actions}</aside></header>`;}
    function notify(message,kind="info"){const item={id:uid("notice"),message:String(message),kind,at:Date.now()};state.notifications.unshift(item);state.notifications=state.notifications.slice(0,60);persist();updateBadge();const toast=$("v15Toast");if(toast){toast.textContent=item.message;toast.dataset.kind=kind;toast.classList.add("show");clearTimeout(notify.timer);notify.timer=setTimeout(()=>toast.classList.remove("show"),3000);}if($("v15Status"))$("v15Status").innerHTML=`<i></i> ${escapeHtml(item.message)}`;tone(kind==="error"?"error":kind==="success"?"accept":"move");}
    function updateBadge(){if($("v15NoticeCount"))$("v15NoticeCount").textContent=String(state.notifications.length);}
    function tone(kind="move"){if(!state.sound||typeof AudioContext==="undefined")return;try{audioContext ||= new AudioContext();const oscillator=audioContext.createOscillator(),gain=audioContext.createGain();oscillator.type="sine";oscillator.frequency.value=kind==="accept"?660:kind==="error"?180:420;gain.gain.setValueAtTime(.025,audioContext.currentTime);gain.gain.exponentialRampToValueAtTime(.0001,audioContext.currentTime+.08);oscillator.connect(gain).connect(audioContext.destination);oscillator.start();oscillator.stop(audioContext.currentTime+.085);}catch(_){}}
    function openNotices(){$("v15SheetKicker").textContent="ACTIVITY CENTER";$("v15SheetTitle").textContent="Notifications";$("v15SheetBody").innerHTML=`<div class="v15-notice-list">${state.notifications.map(item=>`<article class="${item.kind}"><i>${item.kind==="error"?"!":item.kind==="success"?"✓":"•"}</i><span><strong>${escapeHtml(item.message)}</strong><small>${new Date(item.at).toLocaleString()}</small></span></article>`).join("")||"<p>You are all caught up.</p>"}</div><button type="button" data-v15-sheet-action="clear-notices">Clear all</button>`;$("v15SheetBody").onclick=event=>{if(event.target.closest('[data-v15-sheet-action="clear-notices"]')){state.notifications=[];persist();updateBadge();openNotices();}};$("v15Sheet").showModal();}
    function guideCopy(){return{home:["Your production path","Follow the recommended card or jump directly to any area. Every explicit write uses a backup or reversible map layer."],events:["Visual Event Designer","Edit commands with controls, drag them into order, and preserve unknown plugin data without conversion."],tilesets:["Tileset Workshop","Paint flags on rendered tiles, inspect raw sheets, and restore timestamped backups."],live:["Live Production 2.0","A versioned handshake, heartbeat, acknowledgements, and bounded project-local files keep playtest control understandable."],tests:["Journey Test Lab","Record short player journeys, add waits/assertions, and run the complete suite before shipping."],search:["Find anything","Search maps, events, commands, recipes, packs, reviews, and content; preview renames before applying."],intelligence:["World understanding","Passage flags, semantic labels, transfers, events, and autoruns combine into explainable softlock findings."],performance:["Performance Center","Measure event density, recipe and editor operations, save size, and map complexity before optimizing."],extensions:["Security Center","Treat extension code as trusted only when its publisher and permissions are explicit; use Worker probes for Studio contributions."],team:["Collaboration","Compare canonical data, branches, review bundles, and Git changes before selective promotion."],ship:["Release engineering","Stop Live, run every gate, create a fingerprint, and archive the manifest with your deployed project."]}[state.view]||["Creator Guide","Build, verify, and ship with confidence."];}
    function openGuide(){const[title,copy]=guideCopy();$("v15SheetKicker").textContent="PIP'S CREATOR GUIDE";$("v15SheetTitle").textContent=title;$("v15SheetBody").innerHTML=`<div class="v15-guide"><img src="HybridTileGuide.png" alt="Pip guide"><p>${escapeHtml(copy)}</p><div>${[["A","Select"],["X","Commands"],["Y","Guide"],["LB/RB","Areas"]].map(([key,label])=>`<span><kbd>${key}</kbd>${label}</span>`).join("")}</div></div>`;$("v15Sheet").showModal();}

    async function renderHome(){const info=api.projectInfo(),done=Object.values(state.onboarding).filter(Boolean).length,total=Object.keys(state.onboarding).length,next=!info.open?"open-project":!state.onboarding.event?"home-events":!state.onboarding.tileset?"home-tilesets":!state.onboarding.live?"home-live":!state.onboarding.test?"home-tests":!state.onboarding.search?"home-search":"home-ship";$("v15Main").innerHTML=`${pageHeader("CREATOR DASHBOARD","Build boldly. Recover instantly.","A complete world-production loop with visual tools and clear safety rails.",`<button data-v15-action="home-continue" class="v15-primary">Continue</button>`)}
      <section class="v15-home-hero"><div><small>RECOMMENDED NEXT STEP</small><h2>${next==="open-project"?"Open your project":next==="home-events"?"Finish an event visually":next==="home-tilesets"?"Audit your tileset flags":next==="home-live"?"Connect a production playtest":next==="home-tests"?"Record a repeatable journey":next==="home-search"?"Inspect project references":"Run the release gates"}</h2><p>Creator Console keeps powerful systems discoverable and every risky write recoverable.</p><button data-v15-action="${next}" class="v15-primary">Start now →</button></div><aside><div class="v15-progress"><b style="--progress:${Math.round(done/total*100)}%"></b><strong>${done}/${total}</strong><span>production milestones</span></div></aside></section>
      <section class="v15-home-grid">${[["events","◇","Create","Events without parameter JSON"],["tilesets","▦","Shape","Visual flags and semantics"],["live","●","Play","Observe and control safely"],["tests","✓","Prove","Repeat journeys and baselines"],["search","⌕","Navigate","References and safe rename"],["ship","↑","Release","Gated reproducible handoff"]].map(([id,icon,title,copy])=>`<button data-v15-action="home-${id}"><i>${icon}</i><span><strong>${title}</strong><small>${copy}</small></span><b>→</b></button>`).join("")}</section>
      <section class="v15-dashboard"><article><small>SAFETY RAIL</small><h3>${state.recoveryPoints.length} universal restore points</h3><p>Maps, database files, recipes, and Studio metadata can be captured together.</p><button data-v15-action="recovery-create">Create restore point</button></article><article><small>PROJECT PULSE</small><h3>${state.live.connected?"Playtest connected":"Offline authoring"}</h3><p>${Object.keys(state.goldenMaps).length} golden maps · ${state.scenarios.length} journeys · ${state.releaseReports[0]?.ready?"release ready":"checks pending"}</p><button data-v15-action="home-performance">Open Performance</button></article><article><small>COMFORT</small><div class="v15-settings"><label>Theme<select id="v15Theme"><option value="cloud">Cloud</option><option value="midnight">Midnight</option><option value="berry">Berry</option></select></label><label>UI scale<input id="v15Scale" type="range" min="0.85" max="1.25" step="0.05" value="${state.scale}"></label><label><input id="v15Sound" type="checkbox"${state.sound?" checked":""}> Sound feedback</label><label><input id="v15Motion" type="checkbox"${state.reducedMotion?" checked":""}> Reduced motion</label></div></article></section>
      ${state.recoveryPoints.length?`<section class="v15-recovery-strip"><header><small>RECENT RESTORE POINTS</small><button data-v15-action="recovery-inspect">Manage</button></header>${state.recoveryPoints.slice(0,4).map(item=>`<article><i>↶</i><span><strong>${escapeHtml(item.name)}</strong><small>${new Date(item.createdAt).toLocaleString()} · ${item.files?.length||0} files</small></span><button data-v15-recovery="${item.id}">Restore</button></article>`).join("")}</section>`:""}`;
        $("v15Theme").value=state.theme;$("v15Theme").onchange=event=>{state.theme=event.target.value;persist();applyPreferences();};$("v15Scale").oninput=event=>{state.scale=number(event.target.value,1);persist();applyPreferences();};$("v15Sound").onchange=event=>{state.sound=event.target.checked;persist();tone("accept");};$("v15Motion").onchange=event=>{state.reducedMotion=event.target.checked;persist();applyPreferences();};$("v15Main").querySelector(".v15-recovery-strip")?.addEventListener("click",event=>{const button=event.target.closest("[data-v15-recovery]");if(button)restoreRecoveryPoint(button.dataset.v15Recovery);});}

    function commandName(code){return COMMAND_NAMES.get(integer(code))||`Unknown command ${integer(code)}`;}
    function newPage(){return{conditions:{actorId:1,actorValid:false,itemId:1,itemValid:false,selfSwitchCh:"A",selfSwitchValid:false,switch1Id:1,switch1Valid:false,switch2Id:1,switch2Valid:false,variableId:1,variableValid:false,variableValue:0},directionFix:false,image:{characterIndex:0,characterName:"",direction:2,pattern:1,tileId:0},list:[{code:0,indent:0,parameters:[]}],moveFrequency:3,moveRoute:{list:[{code:0,parameters:[]}],repeat:true,skippable:false,wait:false},moveSpeed:3,moveType:0,priorityType:1,stepAnime:false,through:false,trigger:0,walkAnime:true};}
    async function loadEventDraft(){const maps=api.projectInfo().maps;state.selectedMapId=integer(state.selectedMapId||api.state().activeMapId||maps[0]?.id);if(state.selectedMapId)await api.activateMap(state.selectedMapId);currentMap=await api.mapSnapshot(state.selectedMapId||api.state().activeMapId);const events=(currentMap.events||[]).filter(Boolean),preserve=eventDraftDirty&&eventDraft&&eventDraftMapId===state.selectedMapId&&eventDraftEventId===state.selectedEventId;if(!preserve){if(!events.some(item=>item.id===state.selectedEventId))state.selectedEventId=events[0]?.id||0;eventDraft=clone(events.find(item=>item.id===state.selectedEventId)||null);eventDraftMapId=state.selectedMapId;eventDraftEventId=state.selectedEventId;eventDraftDirty=false;}state.selectedPage=clamp(state.selectedPage,0,Math.max(0,(eventDraft?.pages?.length||1)-1));state.selectedCommand=clamp(state.selectedCommand,0,Math.max(0,((eventDraft?.pages?.[state.selectedPage]?.list||[]).length)-1));if(!pluginCommands.length&&api.projectInfo().open)loadPluginCommandMetadata().catch(()=>{});}
    function currentPage(){return eventDraft?.pages?.[state.selectedPage]||null;}
    function valueInput(value,index,label){if(typeof value==="boolean")return`<label>${escapeHtml(label)}<select data-v15-param="${index}" data-value-type="boolean"><option value="true"${value?" selected":""}>On / Yes</option><option value="false"${!value?" selected":""}>Off / No</option></select></label>`;if(typeof value==="number")return`<label>${escapeHtml(label)}<input data-v15-param="${index}" data-value-type="number" type="number" value="${value}"></label>`;if(Array.isArray(value))return`<label>${escapeHtml(label)}<input data-v15-param="${index}" data-value-type="array" value="${escapeHtml(value.join(" | "))}" placeholder="Separate entries with |"></label>`;if(value&&typeof value==="object")return`<fieldset data-v15-param-object="${index}"><legend>${escapeHtml(label)}</legend>${Object.entries(value).map(([key,item])=>`<label>${escapeHtml(key)}<input data-v15-param-object-key="${escapeHtml(key)}" data-value-type="${typeof item}" value="${escapeHtml(item)}"></label>`).join("")||"<small>No object fields.</small>"}</fieldset>`;return`<label>${escapeHtml(label)}${String(value??"").length>80?`<textarea data-v15-param="${index}" data-value-type="string">${escapeHtml(value??"")}</textarea>`:`<input data-v15-param="${index}" data-value-type="string" value="${escapeHtml(value??"")}">`}</label>`;}
    function renderMoveRoute(command){const route=command.parameters?.[1]||{list:[{code:0,parameters:[]}],repeat:true,skippable:false,wait:false};return`<div class="v15-route"><div class="v15-route-options"><label><input id="v15RouteRepeat" type="checkbox"${route.repeat?" checked":""}> Repeat</label><label><input id="v15RouteSkip" type="checkbox"${route.skippable?" checked":""}> Skip if blocked</label><label><input id="v15RouteWait" type="checkbox"${route.wait?" checked":""}> Wait for completion</label></div><div class="v15-route-list">${list(route.list).map((item,index)=>`<article><i>${index+1}</i><span><strong>${escapeHtml(ROUTE_COMMANDS.find(value=>value[0]===integer(item.code))?.[1]||`Move code ${item.code}`)}</strong><small>${escapeHtml(list(item.parameters).join(", "))}</small></span><button data-v15-route-remove="${index}">×</button></article>`).join("")}</div><div class="v15-route-add"><select id="v15RouteCode">${ROUTE_COMMANDS.filter(item=>item[0]!==0).map(([code,name])=>`<option value="${code}">${escapeHtml(name)}</option>`).join("")}</select><input id="v15RouteParams" placeholder="Optional values separated with |"><button data-v15-action="event-route-add">Add movement</button></div></div>`;}
    function renderPluginCommand(command){const[selectedPlugin,selectedCommand,,args={}]=command.parameters||[];return`<div class="v15-plugin-command"><label>Registered command<select id="v15PluginCommand"><option value="">Choose a Plugin Manager command</option>${pluginCommands.map(item=>`<option value="${escapeHtml(`${item.plugin}:${item.command}`)}"${item.plugin===selectedPlugin&&item.command===selectedCommand?" selected":""}>${escapeHtml(item.plugin)} · ${escapeHtml(item.text||item.command)}</option>`).join("")}</select></label><div id="v15PluginArgs">${Object.entries(args||{}).map(([key,value])=>`<label>${escapeHtml(key)}<input data-v15-plugin-arg="${escapeHtml(key)}" value="${escapeHtml(value)}"></label>`).join("")||'<p>Choose a parsed Plugin Manager command to reveal its declared arguments.</p>'}</div><button data-v15-action="event-plugin-refresh">Reload plugin metadata</button></div>`;}
    function parameterEditor(command){if(integer(command.code)===205)return renderMoveRoute(command);if(integer(command.code)===357)return renderPluginCommand(command);const params=list(command.parameters),labels=PARAMETER_LABELS[integer(command.code)]||[];return`<div class="v15-visual-fields">${params.map((value,index)=>valueInput(value,index,labels[index]||`Parameter ${index+1}`)).join("")||'<p>This command has no parameters.</p>'}</div><button data-v15-action="event-param-add">＋ Add parameter</button>`;}
    async function renderEvents(){await loadEventDraft();const maps=api.projectInfo().maps,events=(currentMap.events||[]).filter(Boolean),page=currentPage(),commands=page?.list||[],selected=commands[state.selectedCommand]||{code:0,indent:0,parameters:[]};$("v15Main").innerHTML=`${pageHeader("NO-JSON EVENT AUTHORING","Visual Event Designer","Edit the complete MZ command catalog with direct controls, drag-and-drop structure, and plugin metadata.",`<select id="v15EventMap">${maps.map(item=>`<option value="${item.id}"${item.id===state.selectedMapId?" selected":""}>${escapeHtml(item.name)}</option>`).join("")}</select><button data-v15-action="event-validate">Validate</button><button data-v15-action="event-save" class="v15-primary">Apply Reversibly</button>`)}<section class="v15-event-layout">
      <aside class="v15-event-list"><header><h3>Map events</h3><span>${events.length}</span></header><input id="v15EventSearch" type="search" placeholder="Find an event…">${events.map(item=>`<button data-v15-event="${item.id}" class="${item.id===state.selectedEventId?"active":""}"><i>E${item.id}</i><span><strong>${escapeHtml(item.name||`Event ${item.id}`)}</strong><small>${item.x}, ${item.y} · ${item.pages?.length||0} pages</small></span></button>`).join("")||'<p>No events on this map.</p>'}<button data-v15-action="event-new">＋ New event</button></aside>
      <div class="v15-event-canvas">${eventDraft?`<header><label>Name<input id="v15EventName" value="${escapeHtml(eventDraft.name||"")}"></label><label>X<input id="v15EventX" type="number" value="${eventDraft.x}"></label><label>Y<input id="v15EventY" type="number" value="${eventDraft.y}"></label></header><nav class="v15-pages">${eventDraft.pages.map((_,index)=>`<button data-v15-page="${index}" class="${index===state.selectedPage?"active":""}>Page ${index+1}</button>`).join("")}<button data-v15-action="event-page-add">＋</button></nav><section id="v15CommandTree" class="v15-command-tree">${commands.map((command,index)=>`<button draggable="${integer(command.code)!==0}" data-v15-command-index="${index}" class="${index===state.selectedCommand?"active":""}" style="--indent:${Math.max(0,integer(command.indent))}"><i>${integer(command.code)}</i><span><strong>${escapeHtml(commandName(command.code))}</strong><small>${escapeHtml(commandSummary(command))}</small></span>${!COMMAND_NAMES.has(integer(command.code))?"<b>OPAQUE</b>":""}</button>`).join("")}</section><footer><button data-v15-action="event-up">↑</button><button data-v15-action="event-down">↓</button><button data-v15-action="event-indent">Indent</button><button data-v15-action="event-outdent">Outdent</button><button data-v15-action="event-duplicate">Duplicate</button><button data-v15-action="event-delete" class="v15-danger">Delete</button></footer>`:'<div class="v15-blank"><i>◇</i><h2>Select an event</h2></div>'}</div>
      <aside class="v15-inspector">${eventDraft?`<small>VISUAL COMMAND INSPECTOR</small><h2>${escapeHtml(commandName(selected.code))}</h2><label>Command<select id="v15CommandCode">${EVENT_COMMANDS.map(([code,name,category])=>`<option value="${code}"${integer(selected.code)===code?" selected":""}>${escapeHtml(category)} · ${escapeHtml(name)}</option>`).join("")}</select></label><label>Nesting level<input id="v15CommandIndent" type="number" min="0" max="20" value="${Math.max(0,integer(selected.indent))}"></label>${parameterEditor(selected)}<div class="v15-inspector-actions"><button data-v15-action="event-command-update">Update selected</button><button data-v15-action="event-command-add" class="v15-primary">Add before End</button></div><details><summary>Page conditions and behavior</summary>${pageSettings(page)}</details><output id="v15EventReport">${validateCommands(commands).ok?"Structure is valid.":"Review the command structure."}</output>`:""}</aside></section>`;wireEventView();}
    function commandSummary(command){if(integer(command.code)===357)return`${command.parameters?.[0]||"Plugin"} · ${command.parameters?.[2]||command.parameters?.[1]||"Command"}`;if(integer(command.code)===205)return`${list(command.parameters?.[1]?.list).length||0} route steps`;return list(command.parameters).map(value=>typeof value==="object"?Array.isArray(value)?value.join(" | "):Object.entries(value).map(([key,item])=>`${key}:${item}`).join(" "):String(value)).join(" · ").slice(0,180);}
    function pageSettings(page){const conditions=page?.conditions||{};return`<div class="v15-page-settings"><label>Trigger<select id="v15PageTrigger">${[[0,"Action button"],[1,"Player touch"],[2,"Event touch"],[3,"Autorun"],[4,"Parallel"]].map(([value,label])=>`<option value="${value}"${integer(page?.trigger)===value?" selected":""}>${label}</option>`).join("")}</select></label><label>Priority<select id="v15PagePriority">${[[0,"Below characters"],[1,"Same as characters"],[2,"Above characters"]].map(([value,label])=>`<option value="${value}"${integer(page?.priorityType)===value?" selected":""}>${label}</option>`).join("")}</select></label><label><input id="v15CondSwitch" type="checkbox"${conditions.switch1Valid?" checked":""}> Switch <input id="v15CondSwitchId" type="number" value="${integer(conditions.switch1Id,1)}"></label><label><input id="v15CondVariable" type="checkbox"${conditions.variableValid?" checked":""}> Variable <input id="v15CondVariableId" type="number" value="${integer(conditions.variableId,1)}"> ≥ <input id="v15CondVariableValue" type="number" value="${integer(conditions.variableValue)}"></label><label><input id="v15CondSelf" type="checkbox"${conditions.selfSwitchValid?" checked":""}> Self switch <select id="v15CondSelfValue">${["A","B","C","D"].map(value=>`<option${conditions.selfSwitchCh===value?" selected":""}>${value}</option>`).join("")}</select></label><label><input id="v15WalkAnime" type="checkbox"${page?.walkAnime!==false?" checked":""}> Walking animation</label><label><input id="v15Through" type="checkbox"${page?.through?" checked":""}> Through</label><button data-v15-action="event-page-update">Apply page settings</button></div>`;}
    function validateCommands(commands){const errors=[],warnings=[],stack=[];for(let index=0;index<commands.length;index++){const command=commands[index]||{},code=integer(command.code,-1);if(!Array.isArray(command.parameters))errors.push({index,message:"Parameters must remain an array."});if(!COMMAND_NAMES.has(code))warnings.push({index,message:`Command ${code} is preserved as opaque plugin data.`});if([111,112,102].includes(code))stack.push({code,index});if(code===412&&!stack.some(item=>item.code===111))errors.push({index,message:"End Branch has no Conditional Branch."});if(code===413&&!stack.some(item=>item.code===112))errors.push({index,message:"Repeat Above has no Loop."});if(code===404&&!stack.some(item=>item.code===102))errors.push({index,message:"End Choices has no Show Choices."});}if(!commands.length||integer(commands.at(-1)?.code)!==0)errors.push({index:commands.length,message:"Command list must end with End."});return{ok:!errors.length,errors,warnings,commands:commands.length};}
    function collectVisualCommand(existing){const code=integer($("v15CommandCode").value),command={code,indent:Math.max(0,integer($("v15CommandIndent").value)),parameters:clone(existing?.parameters||[])};if(code===205){const route=command.parameters[1]||{list:[{code:0,parameters:[]}]};route.repeat=!!$("v15RouteRepeat")?.checked;route.skippable=!!$("v15RouteSkip")?.checked;route.wait=!!$("v15RouteWait")?.checked;command.parameters=[integer(command.parameters[0],-1),route];return command;}if(code===357){const selected=pluginCommands.find(item=>`${item.plugin}:${item.command}`===$("v15PluginCommand")?.value);const args={};document.querySelectorAll("[data-v15-plugin-arg]").forEach(input=>args[input.dataset.v15PluginArg]=input.value);command.parameters=selected?[selected.plugin,selected.command,selected.text||selected.command,args]:command.parameters;return command;}const values=[];document.querySelectorAll("[data-v15-param]").forEach(input=>{const index=integer(input.dataset.v15Param),type=input.dataset.valueType;values[index]=type==="boolean"?input.value==="true":type==="number"?number(input.value):type==="array"?String(input.value).split("|").map(value=>parseValue(value.trim())):input.value;});document.querySelectorAll("[data-v15-param-object]").forEach(fieldset=>{const value={};fieldset.querySelectorAll("[data-v15-param-object-key]").forEach(input=>value[input.dataset.v15ParamObjectKey]=input.dataset.valueType==="number"?number(input.value):input.dataset.valueType==="boolean"?input.value==="true":input.value);values[integer(fieldset.dataset.v15ParamObject)]=value;});command.parameters=values;return command;}
    function wireEventView(){if($("v15EventMap"))$("v15EventMap").onchange=safe(async event=>{state.selectedMapId=integer(event.target.value);state.selectedEventId=0;state.selectedPage=0;eventDraftDirty=false;await renderEvents();});$("v15EventSearch")?.addEventListener("input",event=>{const query=event.target.value.toLowerCase();document.querySelectorAll("[data-v15-event]").forEach(item=>item.hidden=!item.textContent.toLowerCase().includes(query));});$("v15Main").querySelector(".v15-event-list")?.addEventListener("click",event=>{const item=event.target.closest("[data-v15-event]");if(item){state.selectedEventId=integer(item.dataset.v15Event);state.selectedPage=0;state.selectedCommand=0;eventDraftDirty=false;renderEvents();}});$("v15Main").querySelector(".v15-pages")?.addEventListener("click",event=>{const page=event.target.closest("[data-v15-page]");if(page){state.selectedPage=integer(page.dataset.v15Page);state.selectedCommand=0;renderEvents();}});const tree=$("v15CommandTree");if(tree){tree.onclick=event=>{const item=event.target.closest("[data-v15-command-index]");if(item){state.selectedCommand=integer(item.dataset.v15CommandIndex);renderEvents();}};tree.ondragstart=event=>{const item=event.target.closest("[data-v15-command-index]");if(item)event.dataTransfer.setData("text/plain",item.dataset.v15CommandIndex);};tree.ondragover=event=>event.preventDefault();tree.ondrop=event=>{event.preventDefault();const from=integer(event.dataTransfer.getData("text/plain"),-1),target=event.target.closest("[data-v15-command-index]"),to=target?integer(target.dataset.v15CommandIndex):-1,page=currentPage();if(from>=0&&to>=0&&from!==to&&integer(page.list[from]?.code)!==0){const[item]=page.list.splice(from,1);page.list.splice(to,0,item);state.selectedCommand=to;eventDraftDirty=true;renderEvents();}};}$("v15PluginCommand")?.addEventListener("change",()=>{const item=pluginCommands.find(value=>`${value.plugin}:${value.command}`===$("v15PluginCommand").value);const command=currentPage().list[state.selectedCommand];if(item){command.parameters=[item.plugin,item.command,item.text||item.command,Object.fromEntries(item.args.map(arg=>[arg.name,arg.default||""]))];eventDraftDirty=true;renderEvents();}});$("v15Main").querySelector(".v15-route")?.addEventListener("click",event=>{const button=event.target.closest("[data-v15-route-remove]");if(!button)return;const command=currentPage().list[state.selectedCommand],route=command?.parameters?.[1];if(!route?.list)return;const index=integer(button.dataset.v15RouteRemove,-1);if(index>=0&&integer(route.list[index]?.code)!==0)route.list.splice(index,1);eventDraftDirty=true;renderEvents();});}
    async function loadPluginCommandMetadata(){pluginCommands=[];let text;try{text=await api.readProjectText("js/plugins.js");}catch(_){return pluginCommands;}const start=text.indexOf("["),end=text.lastIndexOf("]");const plugins=start>=0&&end>start?parseJson(text.slice(start,end+1),[]):[];for(const plugin of list(plugins).filter(item=>item?.status)){try{const source=await api.readProjectText(`js/plugins/${plugin.name}.js`);const matches=[...source.matchAll(/@command\s+([^\r\n]+)([\s\S]*?)(?=\n\s*\*\s*@command|\n\s*\*\/)/g)];for(const match of matches){const body=match[2],command=match[1].trim(),textMatch=body.match(/@text\s+([^\r\n]+)/),args=[...body.matchAll(/@arg\s+([^\r\n]+)([\s\S]*?)(?=\n\s*\*\s*@(arg|command)|$)/g)].map(value=>({name:value[1].trim(),text:(value[2].match(/@text\s+([^\r\n]+)/)?.[1]||value[1]).trim(),type:(value[2].match(/@type\s+([^\r\n]+)/)?.[1]||"string").trim(),default:(value[2].match(/@default\s+([^\r\n]+)/)?.[1]||"").trim()}));pluginCommands.push({plugin:plugin.name,command,text:textMatch?.[1].trim()||command,args});}}catch(_){}}return pluginCommands;}

    function tileBankRange(bank){return{B:[0,256],C:[256,256],D:[512,256],E:[768,256],A5:[1536,128],A1:[2048,256],A2:[2816,256],A3:[4352,256],A4:[5888,256]}[bank]||[0,256];}
    async function loadTilesets(){if(tilesets&&tilesetsDirty)return tilesets;if(api.projectInfo().open){tilesets=await api.readProjectJson("data/Tilesets.json");}else{const map=await api.mapSnapshot();tilesets=[null,{id:map.tilesetId||1,name:`Tileset ${map.tilesetId||1}`,tilesetNames:new Array(9).fill(""),flags:new Array(8192).fill(0)}];}if(!tilesets?.[state.selectedTilesetId])state.selectedTilesetId=tilesets.find(item=>item)?.id||1;return tilesets;}
    async function renderTilesets(){await loadTilesets();const selected=tilesets.find(item=>item?.id===integer(state.selectedTilesetId)),flag=integer(selected?.flags?.[state.selectedTileId]),terrain=(flag>>12)&7,issues=tilesetIssues(selected);$("v15Main").innerHTML=`${pageHeader("VISUAL TILESET WORKSHOP","Paint how the world behaves","See the real tiles, paint passage and terrain flags, inspect semantics, and restore backups.",`<button data-v15-action="tileset-audit">Audit</button><button data-v15-action="tileset-save" class="v15-primary">Save with Backup</button>`)}<section class="v15-tileset-layout"><aside class="v15-tileset-list"><header><h3>Tilesets</h3><span>${tilesets.filter(Boolean).length}</span></header>${tilesets.filter(Boolean).map(item=>`<button data-v15-tileset="${item.id}" class="${item.id===selected.id?"active":""}"><i>▦</i><span><strong>${escapeHtml(item.name)}</strong><small>${list(item.tilesetNames).filter(Boolean).length} sheets</small></span></button>`).join("")}</aside><div class="v15-tileset-work"><header><div><small>TILE BANK</small><nav>${["B","C","D","E","A5","A1","A2","A3","A4"].map(bank=>`<button data-v15-bank="${bank}" class="${state.tileBank===bank?"active":""}">${bank}</button>`).join("")}</nav></div><label>Paint mode<select id="v15TilePaintMode"><option value="inspect">Inspect</option><option value="passable">Passable</option><option value="blocked">Blocked</option><option value="star">Above player ★</option><option value="ladder">Ladder</option><option value="bush">Bush</option><option value="counter">Counter</option><option value="damage">Damage floor</option></select></label></header><div class="v15-tile-canvas-wrap"><canvas id="v15TileBank" width="768" height="768"></canvas><span>Click a rendered tile to inspect or paint its current mode.</span></div><section class="v15-raw-sheets"><header><small>PROJECT SHEETS</small><button data-v15-action="tileset-raw-toggle">Show / hide raw sheets</button></header><div id="v15RawSheets" hidden>${list(selected.tilesetNames).map((name,index)=>name?`<figure data-sheet-name="${escapeHtml(name)}" data-sheet-index="${index}"><div>Loading…</div><figcaption>${["A1","A2","A3","A4","A5","B","C","D","E"][index]} · ${escapeHtml(name)}</figcaption></figure>`:"").join("")}</div></section></div><aside class="v15-tile-inspector"><div class="v15-tile-preview"><canvas id="v15TilePreview" width="144" height="144"></canvas><strong>Tile ${state.selectedTileId}</strong><small>${state.tileBank} bank</small></div><section><small>PASSAGE DIRECTIONS</small><div class="v15-direction-flags">${[[1,"↓"],[2,"←"],[4,"→"],[8,"↑"]].map(([bit,label])=>`<label><input data-v15-flag="${bit}" type="checkbox"${flag&bit?" checked":""}><span>${label}</span></label>`).join("")}</div>${[[16,"Above player ★"],[32,"Ladder"],[64,"Bush"],[128,"Counter"],[256,"Damage floor"]].map(([bit,label])=>`<label class="v15-check"><input data-v15-flag="${bit}" type="checkbox"${flag&bit?" checked":""}> ${label}</label>`).join("")}<label>Terrain tag<select id="v15Terrain">${[0,1,2,3,4,5,6,7].map(value=>`<option value="${value}"${value===terrain?" selected":""}>${value}</option>`).join("")}</select></label><button data-v15-action="tileset-apply-flag" class="v15-primary">Apply flag</button></section><section><small>SEMANTIC MEANING</small><label>Labels<input id="v15SemanticLabels" value="${escapeHtml(list(state.semanticProfiles[selected.id]?.labels?.[state.selectedTileId]).join(", "))}" placeholder="floor, road, water, wall…"></label><button data-v15-action="tileset-semantic">Save labels</button></section><section class="v15-findings"><small>FINDINGS</small>${issues.slice(0,12).map(item=>`<article class="${item.severity}"><i>${item.severity==="error"?"!":"•"}</i><span>${escapeHtml(item.message)}</span></article>`).join("")||"<p>No suspicious flags found.</p>"}</section><button data-v15-action="tileset-backups">Restore a backup</button></aside></section>`;$("v15TilePaintMode").value=state.tilePaintMode;$("v15TilePaintMode").onchange=event=>{state.tilePaintMode=event.target.value;persist();};wireTilesets(selected);drawTileBank(selected);drawTilePreview();}
    function drawTileBank(tileset){const canvas=$("v15TileBank"),context=canvas?.getContext("2d");if(!context)return;context.clearRect(0,0,canvas.width,canvas.height);const[base,count]=tileBankRange(state.tileBank),columns=16,size=48,rows=Math.ceil(count/columns);canvas.height=rows*size;for(let index=0;index<count;index++){const tileId=base+index,x=(index%columns)*size,y=Math.floor(index/columns)*size;api.drawTile(context,tileId,x,y,size);const flag=integer(tileset.flags?.[tileId]);context.strokeStyle=tileId===state.selectedTileId?"#ffca4b":"rgba(255,255,255,.16)";context.lineWidth=tileId===state.selectedTileId?4:1;context.strokeRect(x+.5,y+.5,size-1,size-1);if(flag){context.fillStyle="rgba(10,25,35,.72)";context.fillRect(x+2,y+31,44,15);context.fillStyle="#fff";context.font="9px sans-serif";context.fillText(`${flag&16?"★":""}${flag&15?"×":"○"} T${(flag>>12)&7}`,x+5,y+42);}}}
    function drawTilePreview(){const canvas=$("v15TilePreview"),context=canvas?.getContext("2d");if(context){context.clearRect(0,0,144,144);api.drawTile(context,state.selectedTileId,0,0,144);}}
    function tilesetIssues(tileset){const issues=[];for(let id=0;id<(tileset.flags?.length||0);id++){const flag=integer(tileset.flags[id]);if((flag&16)&&(flag&15)===15)issues.push({severity:"warning",tileId:id,message:`Tile ${id} is both ★ and blocked in every direction.`});if(((flag>>12)&7)>0&&id===0)issues.push({severity:"warning",tileId:id,message:"Empty tile carries a terrain tag."});}for(const[index,name]of list(tileset.tilesetNames).entries())if(!name&&index>=5)issues.push({severity:"info",message:`Optional ${["A1","A2","A3","A4","A5","B","C","D","E"][index]} sheet is empty.`});return issues;}
    function wireTilesets(selected){$("v15Main").querySelector(".v15-tileset-list").onclick=event=>{const item=event.target.closest("[data-v15-tileset]");if(item){state.selectedTilesetId=integer(item.dataset.v15Tileset);state.selectedTileId=0;renderTilesets();}};$("v15Main").querySelector(".v15-tileset-work nav").onclick=event=>{const item=event.target.closest("[data-v15-bank]");if(item){state.tileBank=item.dataset.v15Bank;state.selectedTileId=tileBankRange(state.tileBank)[0];persist();renderTilesets();}};$("v15TileBank").onclick=event=>{const rect=event.currentTarget.getBoundingClientRect(),scaleX=event.currentTarget.width/rect.width,scaleY=event.currentTarget.height/rect.height,x=Math.floor((event.clientX-rect.left)*scaleX/48),y=Math.floor((event.clientY-rect.top)*scaleY/48),[base,count]=tileBankRange(state.tileBank),tileId=base+y*16+x;if(tileId>=base&&tileId<base+count){state.selectedTileId=tileId;if(state.tilePaintMode!=="inspect"){let flag=integer(selected.flags[tileId]);if(state.tilePaintMode==="passable")flag&=~15;else if(state.tilePaintMode==="blocked")flag|=15;else{const bit={star:16,ladder:32,bush:64,counter:128,damage:256}[state.tilePaintMode];flag^=bit;}selected.flags[tileId]=flag;tilesetsDirty=true;}renderTilesets();}};}
    async function loadRawSheets(selected){const container=$("v15RawSheets");if(!container)return;container.hidden=false;for(const figure of container.querySelectorAll("figure")){const index=integer(figure.dataset.sheetIndex),name=figure.dataset.sheetName,extension=await findTilesetExtension(name);if(!extension){figure.querySelector("div").textContent="Missing image";figure.classList.add("missing");continue;}try{const url=await api.projectFileUrl(`img/tilesets/${name}.${extension}`,extension==="png"?"image/png":"image/webp");sheetUrls.push(url);figure.querySelector("div").innerHTML=`<img src="${url}" alt="${escapeHtml(name)}">`;}catch(error){figure.querySelector("div").textContent=error.message;}}}
    async function findTilesetExtension(name){if(!api.projectInfo().open)return"";for(const extension of ["png","webp","jpg","jpeg"]){if(await api.projectEntryExists(`img/tilesets/${name}.${extension}`))return extension;}return"";}

    async function sendLive(type,payload={}){if(!api.projectInfo().open)throw new Error("Open a project before using Live Production.");const command={id:`studio-${Date.now()}-${++state.live.sequence}`,sequence:state.live.sequence,type,payload,createdAt:Date.now(),protocolVersion:2};state.live.commands.push(command);state.live.commands=state.live.commands.slice(-100);await api.writeProjectJson("data/HybridTileLiveCommands.json",{format:"HybridTileLiveCommands",version:2,clientId:"studio-v15",commands:state.live.commands},true);persist();return command;}
    async function pollLive(force=false){if(!api.projectInfo().open)return null;try{const value=await api.readProjectJson("data/HybridTileLiveState.json");state.live.connected=!!value.session;state.live.lastState=value;state.live.protocolMismatch=integer(value.protocolVersion||value.version)!==2;state.live.logs=[...list(value.logs),...state.live.logs].slice(0,100);persist();if(force||state.view==="live")renderLive();return value;}catch(error){state.live.connected=false;if(force)notify("Waiting for the playtest handshake.","info");return null;}}
    async function startLive(){await sendLive("startSession",{label:"Creator Console",clientId:"studio-v15",protocolVersion:2,watchedSwitches:String(state.live.watchedSwitches).split(/[, ]+/).map(Number).filter(Boolean),watchedVariables:String(state.live.watchedVariables).split(/[, ]+/).map(Number).filter(Boolean),staleAfterMs:30000});await sendLive("handshake",{clientId:"studio-v15",protocolVersion:2});let launched=null;try{launched=await api.launchPlaytest({test:true,console:false});}catch(error){notify("Start playtest from RPG Maker; Creator Console is waiting for it.","info");}state.onboarding.live=true;persist();clearInterval(pollTimer);pollTimer=setInterval(()=>pollLive(false),1000);setTimeout(()=>pollLive(true),700);return launched;}
    async function stopLive(){await sendLive("stopSession",{reason:"studio"});state.live.connected=false;state.live.recording=false;clearInterval(pollTimer);pollTimer=0;persist();renderLive();}
    async function renderLive(){const live=state.live.lastState||{},connected=state.live.connected,ack=live.lastAck;$("v15Main").innerHTML=`${pageHeader("VERSIONED PLAYTEST CONNECTION","Live Production 2.0","Handshake, heartbeat, acknowledgements, state diffs, command history, and recovery without a network server.",`<button data-v15-action="live-connect" class="v15-primary">${connected?"Reconnect":"Launch / Attach"}</button><button data-v15-action="live-stop"${connected?"":" disabled"}>Stop</button>`)}${state.live.protocolMismatch?'<div class="v15-banner error"><b>Protocol mismatch</b><span>Studio expects protocol 2. Update the runtime plugin before sending production commands.</span></div>':""}<section class="v15-live-layout"><aside class="v15-live-connection"><div class="v15-live-orb ${connected?"connected":""}"><i>${connected?"✓":"…"}</i></div><small>CONNECTION</small><h2>${connected?"Playtest online":"Waiting for playtest"}</h2><p>${connected?`Heartbeat ${live.heartbeatAt?new Date(live.heartbeatAt).toLocaleTimeString():"pending"}`:"The runtime discovers the pending session command when playtest starts."}</p><dl><div><dt>Protocol</dt><dd>${live.protocolVersion||2}</dd></div><div><dt>Sequence</dt><dd>${live.sequence||state.live.sequence}</dd></div><div><dt>State hash</dt><dd>${escapeHtml(live.stateHash||"—")}</dd></div><div><dt>Acknowledgement</dt><dd class="${ack?.ok===false?"error":""}">${ack?`${ack.id} · ${ack.ok?"OK":"FAILED"}`:"—"}</dd></div></dl><button data-v15-action="live-ping">Ping runtime</button><button data-v15-action="live-clean">${cleanupArmed?"Confirm stale-file cleanup":"Clean stale files"}</button></aside><div class="v15-live-center"><section class="v15-live-map"><header><div><small>RUNTIME POSITION</small><h2>Map ${live.mapId||"—"}</h2></div><b>${live.player?`${live.player.x}, ${live.player.y} · ↓${live.player.direction}`:"Not available"}</b></header><div class="v15-live-player"><span style="--x:${clamp(number(live.player?.x),0,20)};--y:${clamp(number(live.player?.y),0,12)}">◆</span></div></section><section class="v15-live-state"><header><h3>Watched state</h3><button data-v15-action="live-watch">Update watchers</button></header><div><label>Switch IDs<input id="v15WatchSwitches" value="${escapeHtml(state.live.watchedSwitches)}"></label><label>Variable IDs<input id="v15WatchVariables" value="${escapeHtml(state.live.watchedVariables)}"></label></div><div class="v15-state-grid">${Object.entries(live.switches||{}).map(([id,value])=>`<article><i class="${value?"on":""}"></i><span><small>SWITCH ${id}</small><strong>${value?"ON":"OFF"}</strong></span></article>`).join("")}${Object.entries(live.variables||{}).map(([id,value])=>`<article><i>V</i><span><small>VARIABLE ${id}</small><strong>${escapeHtml(typeof value==="object"?JSON.stringify(value):value)}</strong></span></article>`).join("")||'<p>No watched values received.</p>'}</div></section><section class="v15-live-actions">${[["live-reload","↻","Reload recipes"],["live-recovery","↶","Restore point"],["live-pause","Ⅱ","Pause systems"],["live-resume","▶","Resume systems"],["live-diagnostics","⌁","Diagnostics"],["live-import","⇣","Import recording"]].map(([action,icon,label])=>`<button data-v15-action="${action}"${connected?"":" disabled"}><i>${icon}</i><span>${label}</span></button>`).join("")}</section></div><aside class="v15-live-log"><header><small>LIVE LOG</small><button data-v15-action="live-clear-log">Clear</button></header><div>${[...list(live.logs),...state.live.logs].slice(-80).reverse().map(item=>`<article><i>${item.kind==="error"?"!":"•"}</i><span><strong>${escapeHtml(item.type||item.kind||"runtime")}</strong><small>${item.at?new Date(item.at).toLocaleTimeString():""} · #${item.sequence||"—"}</small></span></article>`).join("")||'<p>No runtime messages yet.</p>'}</div><section class="v15-recorder ${state.live.recording?"recording":""}"><i></i><span><small>JOURNEY RECORDER</small><strong>${state.live.recording?"Recording actions":"Ready"}</strong></span><button data-v15-action="${state.live.recording?"record-stop":"record-start"}"${connected?"":" disabled"}>${state.live.recording?"Stop":"Record"}</button></section></aside></section>`;}

    function scenarioFromRecording(recording){const supported=new Set(["move","switch","variable","transfer","interaction","choice","wait","recipe","live-command","event-command"]);return{id:`scenario-${recording.id}`,format:"HybridPlaytestScenario",version:2,studioVersion:"15.0.0",name:recording.name||"Recorded journey",sourceRecordingId:recording.id,createdAt:Date.now(),setup:{mapId:recording.start?.mapId||0,player:clone(recording.start?.player||null),switches:clone(recording.start?.switches||{}),variables:clone(recording.start?.variables||{})},steps:list(recording.events).filter(item=>supported.has(item.type)).map(item=>({type:item.type,mapId:item.mapId,player:clone(item.player),detail:clone(item.detail||{})})),assertions:{mapId:recording.end?.mapId||0,player:clone(recording.end?.player||null),switches:clone(recording.end?.switches||{}),variables:clone(recording.end?.variables||{})}};}
    async function importLastRecording(){const value=await api.readProjectJson("data/HybridTileLastRecording.json");if(value.format!=="HybridPlaytestRecording")throw new Error("The latest runtime file is not a recording.");state.recordings=state.recordings.filter(item=>item.id!==value.id);state.recordings.unshift(value);persist();notify("Latest playtest recording imported.","success");return value;}
    function selectedScenario(){return state.scenarios.find(item=>item.id===state.selectedScenarioId)||state.scenarios[0]||null;}
    async function renderTests(){const scenario=selectedScenario(),golden=Object.entries(state.goldenMaps);$("v15Main").innerHTML=`${pageHeader("AUTOMATED PLAYER JOURNEYS","Journey Test Lab","Replay movement, transfers, interactions, choices, waits, state changes, and assertions; compare golden map state in one suite.",`<button data-v15-action="test-import">Import Recording</button><button data-v15-action="test-run-all" class="v15-primary">Run Complete Suite</button>`)}<section class="v15-test-layout"><aside class="v15-test-list"><header><h3>Journeys</h3><span>${state.scenarios.length}</span></header>${state.scenarios.map(item=>`<button data-v15-scenario="${item.id}" class="${scenario?.id===item.id?"active":""}"><i>✓</i><span><strong>${escapeHtml(item.name)}</strong><small>${item.steps?.length||0} steps</small></span></button>`).join("")||'<p>Record a playtest or create a journey.</p>'}<button data-v15-action="test-new">＋ New journey</button><hr><h3>Recordings</h3>${state.recordings.slice(0,10).map(item=>`<button data-v15-recording="${item.id}"><i>●</i><span><strong>${escapeHtml(item.name)}</strong><small>${item.events?.length||0} actions</small></span></button>`).join("")||'<p>No imported recordings.</p>'}</aside><div class="v15-test-editor">${scenario?`<header><label>Journey name<input id="v15ScenarioName" value="${escapeHtml(scenario.name)}"></label><button data-v15-action="test-export">Export .htgscenario</button><button data-v15-action="test-run" class="v15-primary">Run</button></header><section class="v15-step-list">${list(scenario.steps).map((step,index)=>`<article data-v15-step="${index}"><i>${index+1}</i><span><strong>${escapeHtml(stepLabel(step))}</strong><small>${escapeHtml(stepDetail(step))}</small></span><button data-v15-step-up="${index}">↑</button><button data-v15-step-down="${index}">↓</button><button data-v15-step-remove="${index}">×</button></article>`).join("")||'<p>This journey has no steps yet.</p>'}</section><footer><select id="v15NewStepType">${["move","interaction","transfer","choice","switch","variable","wait","waitUntil","recipe","assert"].map(type=>`<option value="${type}">${type}</option>`).join("")}</select><input id="v15NewStepValue" placeholder="Values: e.g. direction=2,count=3"><button data-v15-action="test-step-add">Add step</button></footer><details><summary>Setup and final assertions</summary>${stateForm("setup",scenario.setup)}${stateForm("assertions",scenario.assertions)}<button data-v15-action="test-state-update">Apply setup/assertions</button></details>`:'<div class="v15-blank"><i>✓</i><h2>Select or create a journey</h2></div>'}</div><aside class="v15-test-results"><small>LAST SUITE</small>${state.scenarioRuns[0]?`<div class="v15-test-score ${state.scenarioRuns[0].passed?"good":"bad"}"><i>${state.scenarioRuns[0].passed?"✓":"!"}</i><strong>${state.scenarioRuns[0].passed?"All tests passed":"Review failures"}</strong><span>${state.scenarioRuns[0].failures||0} failures</span></div>`:'<p>No suite has run in this session.</p>'}<section><header><h3>Golden maps</h3><span>${golden.length}</span></header><button data-v15-action="golden-capture">Capture all</button><button data-v15-action="golden-compare">Compare all</button>${golden.map(([id,item])=>`<article><i>▦</i><span><strong>Map ${id}</strong><small>${escapeHtml(item.checksum)}</small></span></article>`).join("")}</section><section><small>CI / HEADLESS</small><p>Export a deterministic test manifest for the bundled Node validation runner.</p><button data-v15-action="test-cli">Export CI manifest</button></section></aside></section>`;wireTests();}
    function stateForm(prefix,value={}){return`<fieldset class="v15-state-form"><legend>${prefix==="setup"?"Starting state":"Expected final state"}</legend><label>Map ID<input id="v15-${prefix}-map" type="number" value="${integer(value.mapId)}"></label><label>Player X<input id="v15-${prefix}-x" type="number" value="${integer(value.player?.x)}"></label><label>Player Y<input id="v15-${prefix}-y" type="number" value="${integer(value.player?.y)}"></label><label>Switches <input id="v15-${prefix}-switches" value="${escapeHtml(Object.entries(value.switches||{}).map(([id,item])=>`${id}=${item}`).join(", "))}" placeholder="1=true, 2=false"></label><label>Variables <input id="v15-${prefix}-variables" value="${escapeHtml(Object.entries(value.variables||{}).map(([id,item])=>`${id}=${item}`).join(", "))}" placeholder="1=10, 2=ready"></label></fieldset>`;}
    function parsePairs(value){const result={};for(const part of String(value||"").split(",")){const[id,raw]=part.split("=");if(String(id||"").trim())result[String(id).trim()]=parseValue(String(raw??"").trim());}return result;}
    function stepLabel(step){return{move:"Move player",interaction:"Interact",transfer:"Transfer map",choice:"Choose option",switch:"Set switch",variable:"Set variable",wait:"Wait",waituntil:"Wait for condition",recipe:"Run World Recipe",assert:"Assert state"}[String(step.type).toLowerCase()]||step.type;}
    function stepDetail(step){return Object.entries(step.detail||{}).map(([key,value])=>`${key}: ${typeof value==="object"?JSON.stringify(value):value}`).join(" · ").slice(0,180);}
    function parseStep(type,text){const detail={};for(const part of String(text||"").split(",")){const[key,...rest]=part.split("=");if(key.trim())detail[key.trim()]=parseValue(rest.join("=").trim());}return{type,detail};}
    function wireTests(){$("v15Main").querySelector(".v15-test-list").onclick=event=>{const scenario=event.target.closest("[data-v15-scenario]");if(scenario){state.selectedScenarioId=scenario.dataset.v15Scenario;renderTests();}const recording=event.target.closest("[data-v15-recording]");if(recording){const value=state.recordings.find(item=>item.id===recording.dataset.v15Recording),created=scenarioFromRecording(value);state.scenarios=state.scenarios.filter(item=>item.id!==created.id);state.scenarios.unshift(created);state.selectedScenarioId=created.id;persist();renderTests();}};$("v15Main").querySelector(".v15-step-list")?.addEventListener("click",event=>{const scenario=selectedScenario(),remove=event.target.closest("[data-v15-step-remove]"),up=event.target.closest("[data-v15-step-up]"),down=event.target.closest("[data-v15-step-down]");if(remove)scenario.steps.splice(integer(remove.dataset.v15StepRemove),1);if(up){const index=integer(up.dataset.v15StepUp),to=Math.max(0,index-1);[scenario.steps[index],scenario.steps[to]]=[scenario.steps[to],scenario.steps[index]];}if(down){const index=integer(down.dataset.v15StepDown),to=Math.min(scenario.steps.length-1,index+1);[scenario.steps[index],scenario.steps[to]]=[scenario.steps[to],scenario.steps[index]];}persist();renderTests();});}
    async function runStudioSuite(){const runs=[];for(const scenario of state.scenarios){const errors=[];if(!scenario.steps?.length)errors.push("No steps");if(!scenario.assertions)errors.push("No assertions");runs.push({scenarioId:scenario.id,passed:!errors.length,errors});}const golden=await compareGoldenMaps(),failures=runs.filter(item=>!item.passed).length+golden.filter(item=>!item.passed).length;const result={id:uid("suite"),format:"HybridStudioTestRun",version:1,studioVersion:"15.0.0",createdAt:Date.now(),runs,golden,failures,passed:failures===0};state.scenarioRuns.unshift(result);state.scenarioRuns=state.scenarioRuns.slice(0,50);persist();return result;}
    async function captureGoldenMaps(){for(const info of api.projectInfo().maps.length?api.projectInfo().maps:[{id:api.state().activeMapId,name:"Loose map"}]){const map=await api.mapSnapshot(info.id);state.goldenMaps[String(info.id)]={mapId:info.id,name:info.name,checksum:api.checksum(map),capturedAt:Date.now(),width:map.width,height:map.height};}persist();notify("Golden map baselines captured.","success");}
    async function compareGoldenMaps(){const results=[];for(const[itemId,baseline]of Object.entries(state.goldenMaps)){const map=await api.mapSnapshot(integer(itemId)),actual=api.checksum(map);results.push({mapId:integer(itemId),expected:baseline.checksum,actual,passed:baseline.checksum===actual});}return results;}

    async function searchProject(query=state.searchQuery){state.searchQuery=String(query||"").trim();const lower=state.searchQuery.toLowerCase(),results=[];if(!lower){state.searchResults=[];return results;}const maps=api.projectInfo().maps.length?api.projectInfo().maps:[{id:api.state().activeMapId,name:"Loose map"}],previous=api.state().activeMapId;for(const info of maps){const map=await api.mapSnapshot(info.id);if(info.name.toLowerCase().includes(lower))results.push({type:"map",mapId:info.id,title:info.name,path:`Map ${info.id}`,preview:`${map.width}×${map.height}`});for(const event of map.events||[])if(event){if(`${event.name} ${event.note}`.toLowerCase().includes(lower))results.push({type:"event",mapId:info.id,eventId:event.id,title:event.name||`Event ${event.id}`,path:`${info.name} / Event ${event.id}`,preview:event.note||`${event.x}, ${event.y}`});for(let pageIndex=0;pageIndex<(event.pages||[]).length;pageIndex++)for(let commandIndex=0;commandIndex<(event.pages[pageIndex].list||[]).length;commandIndex++){const command=event.pages[pageIndex].list[commandIndex],text=`${commandName(command.code)} ${commandSummary(command)}`;if(text.toLowerCase().includes(lower))results.push({type:"command",mapId:info.id,eventId:event.id,pageIndex,commandIndex,title:commandName(command.code),path:`${info.name} / ${event.name||`Event ${event.id}`} / Page ${pageIndex+1}`,preview:commandSummary(command)});}}}try{for(const item of v14.contentItems().filter(item=>`${item.name} ${item.type} ${item.source}`.toLowerCase().includes(lower)))results.push({type:item.type,title:item.name,path:item.source,preview:list(item.tags).join(", ")});}catch(_){}state.searchResults=results.slice(0,1000);state.onboarding.search=true;if(previous)await api.activateMap(previous);persist();return state.searchResults;}
    async function renderSearch(){const grouped=Object.groupBy?Object.groupBy(state.searchResults,item=>item.type):state.searchResults.reduce((groups,item)=>{(groups[item.type]||=[]).push(item);return groups;},{});$("v15Main").innerHTML=`${pageHeader("PROJECT-WIDE NAVIGATION","Find anything","Search maps, events, commands, world content, reviews, and references; preview every rename before applying.",`<button data-v15-action="search-bookmarks">Bookmarks ${state.bookmarks.length}</button>`)}<section class="v15-search-layout"><div class="v15-search-main"><form id="v15SearchForm"><i>⌕</i><input id="v15SearchQuery" value="${escapeHtml(state.searchQuery)}" placeholder="Try a switch ID, event name, recipe, map, or command…"><button class="v15-primary">Search project</button></form><nav>${Object.entries(grouped).map(([type,items])=>`<button data-v15-search-filter="${type}">${escapeHtml(type)} <b>${items.length}</b></button>`).join("")}</nav><section class="v15-search-results">${state.searchResults.map((item,index)=>`<article data-type="${escapeHtml(item.type)}"><i>${item.type==="map"?"▦":item.type==="event"?"◇":item.type==="command"?"→":"⬡"}</i><span><small>${escapeHtml(item.path)}</small><strong>${escapeHtml(item.title)}</strong><p>${escapeHtml(item.preview||"")}</p></span><button data-v15-result-open="${index}">Open</button><button data-v15-result-bookmark="${index}">${state.bookmarks.some(value=>value.path===item.path)?"★":"☆"}</button></article>`).join("")||'<div class="v15-blank"><i>⌕</i><h2>Search the whole project</h2><p>Results include precise map, event, page, and command locations.</p></div>'}</section></div><aside class="v15-rename"><small>SAFE PROJECT RENAME</small><h2>Preview references first</h2><p>Exact string references can be updated across loaded maps as reversible layers. Project files remain unsaved until Save All.</p><label>Find<input id="v15RenameFrom" value="${escapeHtml(searchPlan?.from||"")}"></label><label>Replace with<input id="v15RenameTo" value="${escapeHtml(searchPlan?.to||"")}"></label><label><input id="v15RenameExact" type="checkbox" checked> Exact strings only</label><button data-v15-action="search-rename-preview">Preview rename</button>${searchPlan?`<div class="v15-rename-plan"><strong>${searchPlan.matches.length} references</strong>${searchPlan.matches.slice(0,30).map(item=>`<span>${escapeHtml(item.path)}</span>`).join("")}<button data-v15-action="search-rename-apply" class="v15-primary"${searchPlan.matches.length?"":" disabled"}>Apply as reversible layers</button></div>`:""}<hr><small>RECENT BOOKMARKS</small>${state.bookmarks.slice(0,12).map(item=>`<button data-v15-bookmark-path="${escapeHtml(item.path)}"><i>★</i><span>${escapeHtml(item.title)}</span></button>`).join("")||"<p>No bookmarks yet.</p>"}</aside></section>`;$("v15SearchForm").onsubmit=safe(async event=>{await searchProject($("v15SearchQuery").value);renderSearch();});$("v15Main").querySelector(".v15-search-results").onclick=event=>{const openButton=event.target.closest("[data-v15-result-open]"),bookmark=event.target.closest("[data-v15-result-bookmark]");if(openButton)openSearchResult(state.searchResults[integer(openButton.dataset.v15ResultOpen)]);if(bookmark){const item=state.searchResults[integer(bookmark.dataset.v15ResultBookmark)];if(state.bookmarks.some(value=>value.path===item.path))state.bookmarks=state.bookmarks.filter(value=>value.path!==item.path);else state.bookmarks.unshift(clone(item));persist();renderSearch();}};$("v15Main").querySelector("nav")?.addEventListener("click",event=>{const button=event.target.closest("[data-v15-search-filter]");if(button)document.querySelectorAll(".v15-search-results article").forEach(item=>item.hidden=item.dataset.type!==button.dataset.v15SearchFilter);});}
    async function openSearchResult(item){if(!item)return;if(item.mapId){await api.activateMap(item.mapId);state.selectedMapId=item.mapId;}if(item.eventId){state.selectedEventId=item.eventId;state.selectedPage=item.pageIndex||0;state.selectedCommand=item.commandIndex||0;switchView("events");}else if(item.mapId)switchView("intelligence");}
    function deepRename(value,from,to,exact=true){if(typeof value==="string")return exact?(value===from?to:value):value.split(from).join(to);if(Array.isArray(value))return value.map(item=>deepRename(item,from,to,exact));if(value&&typeof value==="object")return Object.fromEntries(Object.entries(value).map(([key,item])=>[key,deepRename(item,from,to,exact)]));return value;}
    async function buildRenamePlan(){const from=$("v15RenameFrom").value,to=$("v15RenameTo").value,exact=$("v15RenameExact").checked;if(!from||!to||from===to)throw new Error("Enter two different names.");await searchProject(from);const matches=state.searchResults.filter(item=>exact?item.preview===from||item.title===from||item.preview.includes(`\"${from}\"`):`${item.title} ${item.preview}`.includes(from));searchPlan={from,to,exact,matches};renderSearch();}
    async function applyRenamePlan(){if(!searchPlan?.matches.length)return;await createRecoveryPoint(`Before rename ${searchPlan.from}`);const mapIds=[...new Set(searchPlan.matches.filter(item=>item.mapId).map(item=>item.mapId))];for(const mapId of mapIds){const before=await api.mapSnapshot(mapId),after=deepRename(before,searchPlan.from,searchPlan.to,searchPlan.exact);if(api.checksum(before)!==api.checksum(after)){await api.applyMapSnapshot(after,`Rename ${searchPlan.from} to ${searchPlan.to}`,mapId);recordHistory({kind:"map",mapId,before,after,label:`Rename ${searchPlan.from}`});}}notify(`${searchPlan.matches.length} references updated in reversible layers.`,"success");searchPlan=null;await searchProject(state.searchQuery);renderSearch();}

    function passageAnalysis(map,tileset){const width=map.width,height=map.height,flags=tileset?.flags||[],directions=[{d:2,dx:0,dy:1,bit:1,opposite:8},{d:4,dx:-1,dy:0,bit:2,opposite:4},{d:6,dx:1,dy:0,bit:4,opposite:2},{d:8,dx:0,dy:-1,bit:8,opposite:1}],cells=[],graph=new Map(),oneWay=[];const can=(x,y,bit)=>{if(x<0||y<0||x>=width||y>=height)return false;for(let z=3;z>=0;z--){const tileId=integer(map.data[(z*height+y)*width+x]),flag=integer(flags[tileId]);if(flag&16)continue;return(flag&bit)===0;}return true;};for(let y=0;y<height;y++)for(let x=0;x<width;x++){const key=`${x},${y}`,exits=[];for(const direction of directions){const nx=x+direction.dx,ny=y+direction.dy;if(nx<0||ny<0||nx>=width||ny>=height)continue;const forward=can(x,y,direction.bit),back=can(nx,ny,direction.opposite);if(forward)exits.push(direction.d);if(forward&&back){if(!graph.has(key))graph.set(key,[]);graph.get(key).push(`${nx},${ny}`);}else if(forward!==back)oneWay.push({x,y,direction:direction.d,nx,ny});}cells.push({x,y,exits,passable:!!exits.length});}const components=[],visited=new Set();for(const cell of cells.filter(item=>item.passable)){const start=`${cell.x},${cell.y}`;if(visited.has(start))continue;const queue=[start],members=[];while(queue.length){const key=queue.shift();if(visited.has(key))continue;visited.add(key);members.push(key);queue.push(...(graph.get(key)||[]));}components.push(members);}components.sort((a,b)=>b.length-a.length);return{cells,components,oneWay,reachable:new Set(components[0]||[])};}
    function semanticUnderstanding(map,passage){const categories={},landmarks=[],tileCounts={};for(let y=0;y<map.height;y++)for(let x=0;x<map.width;x++){const id=integer(map.data[y*map.width+x]);tileCounts[id]=(tileCounts[id]||0)+1;for(const label of list(state.semanticProfiles[map.tilesetId]?.labels?.[id]))categories[label]=(categories[label]||0)+1;}for(const event of map.events||[])if(event){const name=String(event.name||""),kind=/door|gate/i.test(name)?"door":/stair|ladder/i.test(name)?"stairs":/chest|treasure/i.test(name)?"treasure":/shop|merchant/i.test(name)?"shop":/exit|transfer/i.test(name)?"exit":"event";landmarks.push({eventId:event.id,name:name||`Event ${event.id}`,kind,x:event.x,y:event.y,reachable:passage.reachable.has(`${event.x},${event.y}`)});}const frequent=Object.entries(tileCounts).sort((a,b)=>b[1]-a[1]).slice(0,12).map(([tileId,count])=>({tileId:integer(tileId),count}));return{categories,landmarks,frequent};}
    async function analyzeIntelligence(){const mapId=integer(state.selectedMapId||api.state().activeMapId||api.projectInfo().maps[0]?.id);if(mapId)await api.activateMap(mapId);const map=await api.mapSnapshot(mapId),allTilesets=api.projectInfo().open?await api.readProjectJson("data/Tilesets.json"):tilesets||[],tileset=allTilesets?.[map.tilesetId]||{flags:[]},passage=passageAnalysis(map,tileset),semantic=semanticUnderstanding(map,passage),issues=[];if(passage.components.length>1)issues.push({severity:"warning",type:"disconnected",message:`${passage.components.length} disconnected passable areas.`});for(const item of semantic.landmarks.filter(item=>!item.reachable))issues.push({severity:"error",type:"unreachable",eventId:item.eventId,message:`${item.name} is unreachable from the primary area.`});for(const edge of passage.oneWay.slice(0,20))issues.push({severity:"warning",type:"one-way",message:`One-way passage at ${edge.x}, ${edge.y}.`});for(const event of map.events||[])if(event)for(const page of event.pages||[])if(integer(page.trigger)===3&&!list(page.list).some(command=>[115,121,123,201].includes(integer(command.code))))issues.push({severity:"warning",type:"autorun",eventId:event.id,message:`${event.name||`Event ${event.id}`} may autorun without an exit condition.`});intelligence={mapId,map,tileset,passage,semantic,issues,score:Math.max(0,100-issues.filter(item=>item.severity==="error").length*15-issues.filter(item=>item.severity==="warning").length*4)};return intelligence;}
    async function renderIntelligence(){await analyzeIntelligence();const maps=api.projectInfo().maps;$("v15Main").innerHTML=`${pageHeader("PASSAGE-AWARE WORLD UNDERSTANDING","Map Intelligence","Combine native tileset flags, semantic labels, transfers, landmarks, and event behavior into explainable findings.",`<select id="v15IntelMap">${maps.map(item=>`<option value="${item.id}"${item.id===intelligence.mapId?" selected":""}>${escapeHtml(item.name)}</option>`).join("")}</select><button data-v15-action="intel-refresh">Rescan</button><button data-v15-action="intel-doctor" class="v15-primary">Preview Repairs</button>`)}<section class="v15-intel-layout"><div class="v15-intel-map"><header><div><small>REACHABILITY & LANDMARKS</small><h2>World comprehension score: ${intelligence.score}</h2></div><nav><button data-v15-heat="reach" class="active">Reachability</button><button data-v15-heat="semantic">Semantics</button><button data-v15-heat="events">Landmarks</button></nav></header><canvas id="v15IntelCanvas" width="960" height="600"></canvas><footer><span><i class="reachable"></i>Reachable</span><span><i class="blocked"></i>Blocked</span><span><i class="island"></i>Disconnected</span><span><i class="landmark"></i>Landmark</span></footer></div><aside class="v15-intel-side"><div class="v15-intel-score ${intelligence.issues.some(item=>item.severity==="error")?"bad":"good"}"><strong>${intelligence.issues.length?`${intelligence.issues.length} findings`:"World understood"}</strong><span>${intelligence.passage.reachable.size}/${intelligence.map.width*intelligence.map.height} cells reachable</span></div><section><small>DETECTED FEATURES</small><div class="v15-chips">${Object.entries(intelligence.semantic.categories).map(([name,count])=>`<span>${escapeHtml(name)} <b>${count}</b></span>`).join("")||intelligence.semantic.frequent.map(item=>`<button data-v15-auto-label="${item.tileId}">Tile ${item.tileId} <b>${item.count}</b></button>`).join("")}</div><button data-v15-action="intel-auto-label">Suggest semantic labels</button></section><section><small>LANDMARKS</small>${intelligence.semantic.landmarks.map(item=>`<article class="${item.reachable?"":"error"}"><i>${item.kind==="door"?"▯":item.kind==="treasure"?"◆":"◇"}</i><span><strong>${escapeHtml(item.name)}</strong><small>${item.kind} · ${item.x}, ${item.y} · ${item.reachable?"reachable":"blocked"}</small></span></article>`).join("")||"<p>No named landmarks.</p>"}</section><section><small>EXPLAINED FINDINGS</small>${intelligence.issues.slice(0,40).map(item=>`<article class="${item.severity}"><i>${item.severity==="error"?"!":"•"}</i><span><strong>${escapeHtml(item.message)}</strong><small>${escapeHtml(item.type)}</small></span>${item.eventId?`<button data-v15-intel-event="${item.eventId}">Open</button>`:""}</article>`).join("")||"<p>No softlock risks detected.</p>"}</section></aside></section>`;$("v15IntelMap").onchange=event=>{state.selectedMapId=integer(event.target.value);renderIntelligence();};drawIntelligence("reach");$("v15Main").querySelector(".v15-intel-map nav").onclick=event=>{const button=event.target.closest("[data-v15-heat]");if(button){document.querySelectorAll("[data-v15-heat]").forEach(item=>item.classList.toggle("active",item===button));drawIntelligence(button.dataset.v15Heat);}};$("v15Main").querySelector(".v15-intel-side").onclick=event=>{const button=event.target.closest("[data-v15-intel-event]");if(button){state.selectedEventId=integer(button.dataset.v15IntelEvent);switchView("events");}};}
    function drawIntelligence(mode){const canvas=$("v15IntelCanvas"),context=canvas?.getContext("2d");if(!context||!intelligence)return;const map=intelligence.map,size=Math.max(3,Math.min(24,Math.floor(Math.min(canvas.width/map.width,canvas.height/map.height)))),offsetX=(canvas.width-map.width*size)/2,offsetY=(canvas.height-map.height*size)/2;context.fillStyle="#13242d";context.fillRect(0,0,canvas.width,canvas.height);for(const cell of intelligence.passage.cells){const key=`${cell.x},${cell.y}`,reachable=intelligence.passage.reachable.has(key);context.fillStyle=mode==="reach"?(reachable?"#4ec69a":cell.passable?"#e29253":"#263943"):mode==="semantic"?(list(state.semanticProfiles[map.tilesetId]?.labels?.[integer(map.data[cell.y*map.width+cell.x])]).length?"#79aaf2":"#263943"):"#263943";context.fillRect(offsetX+cell.x*size,offsetY+cell.y*size,size-1,size-1);}for(const item of intelligence.semantic.landmarks){context.fillStyle=item.reachable?"#ffd055":"#ff7169";context.beginPath();context.arc(offsetX+(item.x+.5)*size,offsetY+(item.y+.5)*size,Math.max(3,size*.35),0,Math.PI*2);context.fill();}}

    async function buildPerformanceReport(allMaps=false){const infos=api.projectInfo().maps.length?api.projectInfo().maps:[{id:api.state().activeMapId,name:"Loose map"}],maps=allMaps?infos:infos.filter(item=>item.id===api.state().activeMapId||item.id===state.selectedMapId).slice(0,1),reports=[],started=performance.now();for(const info of maps){const map=await api.mapSnapshot(info.id),events=(map.events||[]).filter(Boolean),pages=events.flatMap(event=>list(event.pages)),parallel=pages.filter(page=>integer(page.trigger)===4).length,autorun=pages.filter(page=>integer(page.trigger)===3).length,commands=pages.reduce((sum,page)=>sum+list(page.list).length,0),bytes=JSON.stringify(map).length*2;reports.push({mapId:info.id,name:info.name,width:map.width,height:map.height,cells:map.width*map.height,events:events.length,pages:pages.length,parallel,autorun,commands,bytes,score:Math.max(0,100-parallel*5-autorun*6-Math.max(0,events-100)/5)});}const workspace=parseJson(api.canonicalWorkspaceText(),{}),elapsed=performance.now()-started,recommendations=[];for(const map of reports){if(map.parallel>10)recommendations.push({severity:"warning",message:`${map.name}: ${map.parallel} parallel pages can compete for frame time.`});if(map.autorun>4)recommendations.push({severity:"warning",message:`${map.name}: ${map.autorun} autoruns need termination checks.`});if(map.events>150)recommendations.push({severity:"info",message:`${map.name}: consider splitting ${map.events} events across maps or data systems.`});if(map.bytes>4*1024*1024)recommendations.push({severity:"warning",message:`${map.name}: map JSON exceeds 4 MiB.`});}performanceReport={format:"HybridStudioPerformanceReport",version:2,studioVersion:"15.0.0",createdAt:Date.now(),elapsedMs:elapsed,maps:reports,workspaceBytes:JSON.stringify(workspace).length*2,recommendations,score:reports.length?Math.round(reports.reduce((sum,item)=>sum+item.score,0)/reports.length):100};return performanceReport;}
    async function renderPerformance(){if(!performanceReport)await buildPerformanceReport(false);$("v15Main").innerHTML=`${pageHeader("MEASURE BEFORE OPTIMIZING","Performance Center","Profile map complexity, event density, save metadata, and authoring operations with measurable recommendations.",`<button data-v15-action="perf-current">Profile current map</button><button data-v15-action="perf-all" class="v15-primary">Profile all maps</button>`)}<section class="v15-performance-layout"><div class="v15-performance-main"><div class="v15-performance-hero"><div class="v15-meter" style="--score:${performanceReport.score}"><strong>${performanceReport.score}</strong><span>project score</span></div><div><small>LAST BENCHMARK</small><h2>${performanceReport.maps.length} map${performanceReport.maps.length===1?"":"s"} in ${performanceReport.elapsedMs.toFixed(1)}ms</h2><p>Workspace metadata ${(performanceReport.workspaceBytes/1024).toFixed(1)} KiB</p></div><button data-v15-action="perf-export">Export report</button></div><canvas id="v15FlameChart" width="1100" height="300"></canvas><section class="v15-performance-table"><header><span>Map</span><span>Cells</span><span>Events</span><span>Parallel</span><span>Commands</span><span>Score</span></header>${performanceReport.maps.map(item=>`<article><strong>${escapeHtml(item.name)}</strong><span>${item.cells.toLocaleString()}</span><span>${item.events}</span><span class="${item.parallel>10?"warn":""}">${item.parallel}</span><span>${item.commands}</span><b>${Math.round(item.score)}</b></article>`).join("")}</section></div><aside class="v15-performance-side"><small>RECOMMENDATIONS</small>${performanceReport.recommendations.map(item=>`<article class="${item.severity}"><i>${item.severity==="warning"?"!":"•"}</i><span>${escapeHtml(item.message)}</span></article>`).join("")||'<div class="v15-success"><i>✓</i><strong>No obvious hotspots</strong><span>Keep measuring representative playtests.</span></div>'}<hr><small>PRODUCTION TOOLS</small><button data-v15-action="perf-legacy">Open runtime benchmark lab</button><button data-v15-action="perf-recovery">Create restore point</button><button data-v15-action="perf-compat">Run compatibility profiles</button></aside></section>`;drawFlameChart();}
    function drawFlameChart(){const canvas=$("v15FlameChart"),context=canvas?.getContext("2d");if(!context||!performanceReport)return;context.clearRect(0,0,canvas.width,canvas.height);context.fillStyle="#142a34";context.fillRect(0,0,canvas.width,canvas.height);const max=Math.max(1,...performanceReport.maps.map(item=>item.commands+item.events*4));performanceReport.maps.forEach((item,index)=>{const y=28+index*42,width=(item.commands+item.events*4)/max*(canvas.width-220);context.fillStyle=index%2?"#61caa5":"#73aef0";context.fillRect(180,y,width,26);context.fillStyle="#eaf5f2";context.font="12px sans-serif";context.fillText(item.name,14,y+18);context.fillText(`${item.commands} commands`,190+width,y+18);});}

    async function loadExtensionData(){const manifests=[];if(api.projectInfo().open){try{for(const entry of await api.listProjectDirectory(".hybrid/extensions"))if(entry.kind==="file"&&entry.name.endsWith(".json")){try{manifests.push(await api.readProjectJson(entry.path));}catch(_){}}}catch(_){}}state.extensionManifests=manifests;try{state.extensionSecurity=Object.assign({},await api.readProjectJson(".hybrid/extension-security-v15.json"),state.extensionSecurity);}catch(_){}return manifests;}
    async function probeExtension(manifest){if(typeof Worker==="undefined")return{ok:false,error:"Web Workers are unavailable."};const source=`onmessage=e=>{const m=e.data,errors=[];if(!m||typeof m!=="object")errors.push("Manifest must be an object");if(!m.id)errors.push("Missing id");if(!m.version)errors.push("Missing version");const permissions=Array.isArray(m.permissions)?m.permissions:[];postMessage({ok:!errors.length,errors,permissions,bytes:JSON.stringify(m).length*2,hash:"worker-"+JSON.stringify(m).length});}`;const url=URL.createObjectURL(new Blob([source],{type:"text/javascript"})),worker=new Worker(url);return new Promise(resolve=>{const timeout=setTimeout(()=>{worker.terminate();URL.revokeObjectURL(url);resolve({ok:false,error:"Worker probe exceeded its time budget."});},1000);worker.onmessage=event=>{clearTimeout(timeout);worker.terminate();URL.revokeObjectURL(url);resolve(event.data);};worker.onerror=event=>{clearTimeout(timeout);worker.terminate();URL.revokeObjectURL(url);resolve({ok:false,error:event.message});};worker.postMessage(clone(manifest));});}
    async function renderExtensions(){const manifests=await loadExtensionData(),selected=manifests.find(item=>item.id===state.selectedExtensionId)||manifests[0]||null,security=selected?state.extensionSecurity[selected.id]||{isolation:"worker",network:false,fileRead:[],fileWrite:[],timeBudgetMs:16,payloadBudgetKb:1024,quarantined:false,failures:[]}:null;$("v15Main").innerHTML=`${pageHeader("CAPABILITIES, PUBLISHERS & ISOLATION","Extension Security Center","Review declared access, probe Studio contributions in a Worker, track failures, quarantine, update, and roll back.",`<button data-v15-action="extension-import">Install Manifest</button><button data-v15-action="extension-probe" class="v15-primary"${selected?"":" disabled"}>Run Worker Probe</button>`)}<section class="v15-extension-layout"><aside class="v15-extension-list"><header><h3>Extensions</h3><span>${manifests.length}</span></header>${manifests.map(item=>{const policy=state.extensionSecurity[item.id]||{};return`<button data-v15-extension="${escapeHtml(item.id)}" class="${item.id===selected?.id?"active":""}"><i>${policy.quarantined?"!":"⬡"}</i><span><strong>${escapeHtml(item.name||item.id)}</strong><small>v${escapeHtml(item.version||"0")} · ${policy.isolation||"unconfigured"}</small></span></button>`;}).join("")||'<p>No extension manifests installed.</p>'}<button data-v15-action="extension-compat">Compatibility profiles</button></aside><div class="v15-extension-main">${selected?`<header><div class="v15-extension-icon">⬡</div><div><small>${escapeHtml(selected.publisher||"LOCAL EXTENSION")}</small><h2>${escapeHtml(selected.name||selected.id)}</h2><p>${escapeHtml(selected.description||"No description supplied.")}</p></div><b>v${escapeHtml(selected.version||"0")}</b></header><section class="v15-capabilities"><small>DECLARED CAPABILITIES</small><div>${list(selected.permissions).map(permission=>`<span class="${/write|manage|network/.test(permission)?"sensitive":""}">${escapeHtml(permission)}</span>`).join("")||"<span>No permissions</span>"}</div></section><section class="v15-security-form"><label>Isolation<select id="v15ExtIsolation"><option value="worker">Dedicated Worker</option><option value="process">Desktop child process</option><option value="trusted">Trusted main process</option></select></label><label>Time budget (ms)<input id="v15ExtTime" type="number" min="1" max="5000" value="${integer(security.timeBudgetMs,16)}"></label><label>Payload budget (KiB)<input id="v15ExtPayload" type="number" min="64" max="10240" value="${integer(security.payloadBudgetKb,1024)}"></label><label><input id="v15ExtNetwork" type="checkbox"${security.network?" checked":""}> Allow network broker</label><label>Readable project paths<input id="v15ExtRead" value="${escapeHtml(list(security.fileRead).join(", "))}" placeholder="data/, img/tilesets/"></label><label>Writable project paths<input id="v15ExtWrite" value="${escapeHtml(list(security.fileWrite).join(", "))}" placeholder=".hybrid/extensions/data/"></label><button data-v15-action="extension-policy" class="v15-primary">Save security policy</button></section><section class="v15-publisher"><small>PUBLISHER IDENTITY</small><label>Publisher ID<input id="v15PublisherId" value="${escapeHtml(security.publisherId||selected.publisher||"")}"></label><label>Local fingerprint<input id="v15PublisherFingerprint" value="${escapeHtml(state.publishers[security.publisherId||selected.publisher]?.fingerprint||"")}"></label><label><input id="v15PublisherTrusted" type="checkbox"${state.publishers[security.publisherId||selected.publisher]?.trusted?" checked":""}> Trust this publisher locally</label><button data-v15-action="extension-publisher">Save publisher</button></section>`:'<div class="v15-blank"><i>⬡</i><h2>Install an extension manifest</h2></div>'}</div><aside class="v15-extension-audit">${selected?`<div class="v15-security-seal ${security.quarantined?"bad":state.extensionProbe?.ok?"good":""}"><i>${security.quarantined?"!":state.extensionProbe?.ok?"✓":"…"}</i><strong>${security.quarantined?"Quarantined":state.extensionProbe?.ok?"Probe passed":"Not probed"}</strong><span>${state.extensionProbe?.bytes?`${state.extensionProbe.bytes} bytes inspected`:"Run the isolated manifest probe."}</span></div><button data-v15-action="extension-quarantine" class="${security.quarantined?"":"v15-danger"}">${security.quarantined?"Clear quarantine":"Quarantine extension"}</button><section><small>FAILURE HISTORY</small>${list(security.failures).slice(-10).reverse().map(item=>`<article><i>!</i><span><strong>${escapeHtml(item.error)}</strong><small>${new Date(item.at).toLocaleString()}</small></span></article>`).join("")||"<p>No failures recorded.</p>"}</section><section><small>UPDATES & ROLLBACK</small><p>Manifest replacement creates a timestamped project backup first.</p><button data-v15-action="extension-backups">Inspect backups</button></section>`:""}</aside></section>`;if($("v15ExtIsolation"))$("v15ExtIsolation").value=security.isolation||"worker";$("v15Main").querySelector(".v15-extension-list").onclick=event=>{const item=event.target.closest("[data-v15-extension]");if(item){state.selectedExtensionId=item.dataset.v15Extension;renderExtensions();}};}

    async function createReviewBundle(){const production=api.getExtensionData("production-v9",{}),bundle={id:uid("review"),format:"HybridCollaborationBundle",version:2,studioVersion:"15.0.0",name:`${api.projectInfo().name||"Project"} review`,createdAt:new Date().toISOString(),branch:api.state().activeBranchId||"main",maps:api.projectInfo().maps.map(item=>({id:item.id,name:item.name})),reviewThreads:clone(list(production.reviewThreads)),snapshots:api.listSnapshots(),goldenMaps:clone(state.goldenMaps),release:state.releaseReports[0]||null};bundle.fingerprint=api.checksum(bundle);state.reviewBundles.unshift(bundle);state.reviewBundles=state.reviewBundles.slice(0,30);persist();api.download(`${safeId(bundle.name)}.htgreview`,JSON.stringify(bundle,null,2));notify("Portable review bundle created.","success");return bundle;}
    function compareBundles(a,b){const normalizeItems=items=>new Map(list(items).map(item=>[String(item.id??api.checksum(item)),item])),compare=(left,right)=>{const x=normalizeItems(left),y=normalizeItems(right),added=[],removed=[],changed=[];for(const[key,value]of y)if(!x.has(key))added.push(value);else if(api.checksum(x.get(key))!==api.checksum(value))changed.push({id:key,from:x.get(key),to:value});for(const[key,value]of x)if(!y.has(key))removed.push(value);return{added,removed,changed};};return{from:a.id,to:b.id,maps:compare(a.maps,b.maps),reviews:compare(a.reviewThreads,b.reviewThreads),changed:a.fingerprint!==b.fingerprint};}
    async function renderTeam(){const production=api.getExtensionData("production-v9",{}),bundles=[...state.reviewBundles,...state.importedBundles.filter(item=>item.format==="HybridCollaborationBundle")],comparison=state.bundleComparison;$("v15Main").innerHTML=`${pageHeader("BRANCHES, REVIEWS & VISUAL DIFF","Team Workspace","Understand canonical changes, compare review bundles, inspect Git, and promote work selectively.",`<button data-v15-action="team-git">Refresh Git</button><button data-v15-action="team-bundle" class="v15-primary">Create Review Bundle</button>`)}<section class="v15-team-layout"><div class="v15-team-main"><section class="v15-branch-hero"><div><small>ACTIVE VISUAL BRANCH</small><h2>${escapeHtml(api.state().activeBranchId||"main")}</h2><p>${api.state().branches.length} branches · ${api.listSnapshots().length} snapshots · ${list(production.reviewThreads).length} review threads</p></div><button data-v15-action="team-branch">＋ New branch</button></section><div class="v15-team-columns"><section><header><h3>Branches</h3></header>${api.state().branches.map(item=>`<article><i>${item.id===api.state().activeBranchId?"●":"◇"}</i><span><strong>${escapeHtml(item.name)}</strong><small>${new Date(item.createdAt).toLocaleString()}</small></span><button data-v15-branch="${item.id}">Open</button></article>`).join("")}</section><section><header><h3>Review threads</h3><button data-v15-action="team-reviews">Open classic reviews</button></header>${list(production.reviewThreads).map(item=>`<article><i>${item.status==="resolved"?"✓":"!"}</i><span><strong>${escapeHtml(item.text||item.title||"Review")}</strong><small>${item.status||"open"} · Map ${item.mapId||"—"}</small></span></article>`).join("")||"<p>No review threads.</p>"}</section></div><section class="v15-git"><header><div><small>CANONICAL GIT WORKSPACE</small><h3>Working tree</h3></div><button data-v15-action="team-diff">Load visual diff</button></header><pre id="v15GitOutput">${escapeHtml(state.gitOutput||"Git status has not been checked.")}</pre>${state.gitDiff?`<div class="v15-diff-summary">${state.gitDiff.map(item=>`<article class="${item.kind}"><i>${item.kind==="add"?"+":item.kind==="remove"?"−":"•"}</i><code>${escapeHtml(item.text)}</code></article>`).join("")}</div>`:""}</section></div><aside class="v15-bundle-compare"><small>PORTABLE BUNDLE COMPARISON</small><h2>Compare handoffs</h2><select id="v15BundleA">${bundles.map((item,index)=>`<option value="${index}">${escapeHtml(item.name||item.id)}</option>`).join("")}</select><select id="v15BundleB">${bundles.map((item,index)=>`<option value="${index}"${index===1?" selected":""}>${escapeHtml(item.name||item.id)}</option>`).join("")}</select><button data-v15-action="team-compare"${bundles.length>=2?"":" disabled"}>Compare bundles</button><button data-v15-action="team-import">Import bundle</button>${comparison?`<section class="v15-compare-result"><div><strong>${comparison.maps.added.length}</strong><span>maps added</span></div><div><strong>${comparison.maps.removed.length}</strong><span>maps removed</span></div><div><strong>${comparison.reviews.changed.length}</strong><span>review conflicts</span></div><h3>${comparison.changed?"Handoffs differ":"Handoffs match"}</h3>${comparison.reviews.changed.map(item=>`<article><i>!</i><span><strong>Review ${escapeHtml(item.id)}</strong><small>Manual decision required</small></span></article>`).join("")}<button data-v15-action="team-merge-plan">Export merge plan</button></section>`:""}</aside></section>`;$("v15Main").querySelector(".v15-team-columns")?.addEventListener("click",event=>{const button=event.target.closest("[data-v15-branch]");if(button){api.switchBranch(button.dataset.v15Branch);renderTeam();}});}

    async function runCompatibility(){let plugins=[];try{const text=await api.readProjectText("js/plugins.js"),start=text.indexOf("["),end=text.lastIndexOf("]");plugins=parseJson(text.slice(start,end+1),[]);}catch(_){}const builtins=[{id:"visustella-public",name:"VisuStella public compatibility",match:"VisuMZ",guidance:"Keep HybridTileGraft after foundational engine plugins; verify public tilemap and movement hooks in playtest."},{id:"movement-public",name:"Movement plugin compatibility",match:"movement",guidance:"Test diagonal passage, collision, and transfer arrival using the actual plugin stack."},{id:"lighting-public",name:"Lighting plugin compatibility",match:"light",guidance:"Confirm map reload and live snapshots do not reset lighting state."},{id:"minimap-public",name:"Minimap compatibility",match:"minimap",guidance:"Refresh cached minimap data after runtime tile changes."}],profiles=[...builtins,...state.compatibilityProfiles],results=profiles.map(profile=>{const detected=plugins.filter(item=>`${item.name} ${item.description}`.toLowerCase().includes(profile.match.toLowerCase()));return{...profile,detected:detected.map(item=>item.name),passed:true};});state.compatibilityReport={format:"HybridCompatibilityProfileRun",version:1,studioVersion:"15.0.0",createdAt:Date.now(),plugins:plugins.map(item=>item.name),results,passed:true};persist();return state.compatibilityReport;}
    async function runShipCheck(){const audit=await api.auditProject(),tests=await runStudioSuite(),performance=await buildPerformanceReport(true),compatibility=await runCompatibility(),bridgeFiles=[];for(const path of ["data/HybridTileLiveState.json","data/HybridTileLiveCommands.json","data/HybridTileLastRecording.json"])if(await api.projectEntryExists(path))bridgeFiles.push(path);const quarantined=Object.values(state.extensionSecurity).filter(item=>item.quarantined).length,errors=integer(audit.errors)+(tests.passed?0:tests.failures)+(state.live.connected?1:0)+quarantined,warnings=integer(audit.warnings)+performance.recommendations.length+bridgeFiles.length,fingerprint=api.checksum({workspace:parseJson(api.canonicalWorkspaceText(),{}),golden:state.goldenMaps,extensions:state.extensionSecurity,compatibility:compatibility.results,tests:state.scenarios.map(item=>api.checksum(item))}),report={id:uid("release"),format:"HybridTileStudioReleaseReport",version:3,studioVersion:"15.0.0",createdAt:new Date().toISOString(),project:api.projectInfo(),audit,tests,performance,compatibility,bridgeFiles,quarantined,liveStopped:!state.live.connected,errors,warnings,ready:errors===0,fingerprint};state.releaseReports.unshift(report);state.releaseReports=state.releaseReports.slice(0,30);state.onboarding.ship=true;persist();return report;}
    async function renderShip(){const latest=state.releaseReports[0],fingerprints=state.releaseFingerprints;$("v15Main").innerHTML=`${pageHeader("GATED, REPRODUCIBLE HANDOFF","Ship Your World","Run every production gate, explain changes between fingerprints, prepare deployment exclusions, and export CI/signing manifests.",`<button data-v15-action="ship-runtime">Install Runtime</button><button data-v15-action="ship-check" class="v15-primary">Run Every Gate</button>`)}<section class="v15-ship-layout"><div class="v15-release-road">${[[1,"Protect","Create a universal restore point","ship-recovery"],[2,"Prove","Run journeys, golden maps, compatibility, and performance","ship-check"],[3,"Clean","Remove Live bridge and exclude authoring-only files","ship-clean"],[4,"Identify","Create a reproducible release fingerprint","ship-fingerprint"],[5,"Package","Export CI, update-channel, and signing manifests","ship-manifest"]].map(([number,title,copy,action])=>`<article><i>${number}</i><span><strong>${title}</strong><p>${copy}</p></span><button data-v15-action="${action}">Open</button></article>`).join("")}</div><aside class="v15-release-card"><div class="v15-release-seal ${latest?.ready?"ready":latest?"attention":""}"><i>${latest?.ready?"✓":latest?"!":"…"}</i></div><small>LATEST RELEASE CHECK</small><h2>${latest?latest.ready?"Ready to package":"Review required":"Not checked"}</h2><p>${latest?`${latest.errors} errors · ${latest.warnings} notes · ${new Date(latest.createdAt).toLocaleString()}`:"Run after the production playtest."}</p>${latest?`<code>${escapeHtml(latest.fingerprint)}</code><button data-v15-action="ship-report">Export report</button>`:""}</aside><section class="v15-gates"><header><small>PRODUCTION GATES</small><h2>Nothing important stays implicit</h2></header>${[["Live Production stopped",latest?.liveStopped],["Project dependency audit",latest?integer(latest.audit.errors)===0:null],["Journey and golden suite",latest?.tests?.passed],["Extensions not quarantined",latest?latest.quarantined===0:null],["Compatibility profiles",latest?.compatibility?.passed],["Performance reviewed",latest?latest.performance.recommendations.length===0:null]].map(([label,value])=>`<article class="${value===true?"good":value===false?"bad":""}"><i>${value===true?"✓":value===false?"!":"…"}</i><span>${label}</span></article>`).join("")}</section><section class="v15-fingerprint-compare"><small>WHAT CHANGED BETWEEN RELEASES?</small><h2>Fingerprint comparison</h2><div><select id="v15FingerprintA">${fingerprints.map((item,index)=>`<option value="${index}">${escapeHtml(item.channel)} · ${new Date(item.createdAt).toLocaleString()}</option>`).join("")}</select><select id="v15FingerprintB">${fingerprints.map((item,index)=>`<option value="${index}"${index===1?" selected":""}>${escapeHtml(item.channel)} · ${new Date(item.createdAt).toLocaleString()}</option>`).join("")}</select><button data-v15-action="ship-compare"${fingerprints.length>=2?"":" disabled"}>Explain differences</button></div>${state.releaseComparison?`<div class="v15-release-diff">${state.releaseComparison.changes.map(item=>`<article><i>↔</i><span><strong>${escapeHtml(item.field)}</strong><small>${escapeHtml(item.from)} → ${escapeHtml(item.to)}</small></span></article>`).join("")||"<p>These fingerprints are identical.</p>"}</div>`:""}</section><section class="v15-platforms"><header><div><small>DESKTOP & CI TARGETS</small><h2>Release pipeline</h2></div><label>Channel<select id="v15ReleaseChannel"><option>stable</option><option>preview</option><option>nightly</option></select></label></header>${[["Windows","NSIS · portable · ZIP","Certificate recommended"],["macOS","DMG · ZIP","Developer ID + notarization"],["Linux","AppImage · tar.gz","Optional signing"]].map(([name,targets,note])=>`<article><i>${name[0]}</i><span><strong>${name}</strong><small>${targets}</small></span><em>${note}</em><b>CONFIGURED</b></article>`).join("")}<footer><p>Protected CI secrets stay outside the project. The manifest records requirements, never credentials.</p><button data-v15-action="ship-ci">Export CI templates</button></footer></section></section>`;}

    async function transactionalWrite(path,text,label){const before=await api.projectEntryExists(path)?await api.readProjectText(path):null,id=uid("write"),backup=`.hybrid/recovery/${id}/${path.replaceAll("/","__")}.bak`,journal={format:"HybridStudioTransaction",version:1,id,label,path,backup,createdAt:new Date().toISOString(),status:"writing"};if(before!==null)await api.writeProjectText(backup,before);await api.writeProjectJson(".hybrid/v15-active-transaction.json",journal,true);await api.writeProjectText(path,text);journal.status="complete";journal.completedAt=new Date().toISOString();await api.writeProjectJson(`.hybrid/recovery/${id}/manifest.json`,journal,true);try{await api.removeProjectEntry(".hybrid/v15-active-transaction.json");}catch(_){}recordHistory({kind:"file",path,before,after:String(text),label});return journal;}
    async function inspectCrashJournal(){try{const journal=await api.readProjectJson(".hybrid/v15-active-transaction.json");notify(`Interrupted write detected: ${journal.label||journal.path}. Open recovery before continuing.`,"error");state.crashJournal=journal;persist();return journal;}catch(_){state.crashJournal=null;return null;}}
    async function createRecoveryPoint(name=`Restore point ${new Date().toLocaleString()}`){if(!api.projectInfo().open){const snapshot=api.createSnapshot(name);const point={id:snapshot.id,name,createdAt:Date.now(),files:[],loose:true};state.recoveryPoints.unshift(point);persist();notify("Map restore point created.","success");return point;}const id=uid("recovery"),files=["data/Tilesets.json","data/System.json","data/HybridWorldRecipes.json",".hybrid/HybridTileStudio.json"],maps=api.projectInfo().maps.map(item=>`data/Map${String(item.id).padStart(3,"0")}.json`);for(const path of [...files,...maps])if(await api.projectEntryExists(path)){const text=await api.readProjectText(path);await api.writeProjectText(`.hybrid/recovery/${id}/${path.replaceAll("/","__")}.bak`,text);}const present=[];for(const path of [...files,...maps])if(await api.projectEntryExists(path))present.push({path,backup:`.hybrid/recovery/${id}/${path.replaceAll("/","__")}.bak`});const point={id,format:"HybridUniversalRecoveryPoint",version:1,studioVersion:"15.0.0",name,createdAt:Date.now(),files:present,workspaceHash:api.checksum(parseJson(api.canonicalWorkspaceText(),{}))};await api.writeProjectJson(`.hybrid/recovery/${id}/manifest.json`,point,true);state.recoveryPoints.unshift(point);state.recoveryPoints=state.recoveryPoints.slice(0,20);persist();notify(`Universal restore point captured ${present.length} files.`,"success");return point;}
    async function restoreRecoveryPoint(id){const point=state.recoveryPoints.find(item=>item.id===id);if(!point)throw new Error("Restore point was not found.");if(point.loose){notify("Open the classic snapshot browser to restore this loose-map point.","info");return;}if(point.restoreArmed!==true){point.restoreArmed=true;persist();notify(`Press Restore again to restore ${point.files.length} backed-up files.`,"info");renderHome();return;}for(const file of point.files){const text=await api.readProjectText(file.backup);await api.writeProjectText(file.path,text);}point.restoreArmed=false;point.lastRestoredAt=Date.now();persist();notify("Universal restore completed. Reopen affected maps to refresh them.","success");}
    function recordHistory(item){history.push(item);history=history.slice(-50);redoHistory=[];updateChrome();}
    async function applyHistory(item,direction){const value=direction==="undo"?item.before:item.after;if(item.kind==="map")await api.applyMapSnapshot(clone(value),`${direction}: ${item.label}`,item.mapId);else if(item.kind==="file"){if(value===null){if(await api.projectEntryExists(item.path))await api.removeProjectEntry(item.path);}else await api.writeProjectText(item.path,value);}notify(`${direction==="undo"?"Undid":"Redid"} ${item.label}.`,"success");}
    async function undoUniversal(){const item=history.pop();if(!item)return;await applyHistory(item,"undo");redoHistory.push(item);updateChrome();if(state.view!=="home")render(state.view);}
    async function redoUniversal(){const item=redoHistory.pop();if(!item)return;await applyHistory(item,"redo");history.push(item);updateChrome();if(state.view!=="home")render(state.view);}

    async function runAction(action,button){
        if(action.startsWith("event-")&&!['event-save','event-new','event-validate','event-plugin-refresh'].includes(action))eventDraftDirty=true;
        if(action==="open-project")return api.openProject();if(action==="practice")return openPractice();if(action==="home-continue"){const next=!api.projectInfo().open?"open-project":!state.onboarding.event?"home-events":!state.onboarding.tileset?"home-tilesets":!state.onboarding.live?"home-live":!state.onboarding.test?"home-tests":!state.onboarding.search?"home-search":"home-ship";return runAction(next);}if(action.startsWith("home-"))return switchView(action.slice(5));
        if(action==="recovery-create"||action==="perf-recovery"||action==="ship-recovery")return createRecoveryPoint();if(action==="recovery-inspect"){switchView("home");return notify("Recent restore points are shown below the dashboard.");}
        if(action==="event-validate"){const report=validateCommands(currentPage()?.list||[]);$("v15EventReport").textContent=report.ok?`Valid: ${report.commands} commands and ${report.warnings.length} preserved note(s).`:report.errors.map(item=>item.message).join(" ");return notify(report.ok?"Event structure passed validation.":`${report.errors.length} event errors found.`,report.ok?"success":"error");}
        if(action==="event-save"){const before=await api.mapSnapshot(state.selectedMapId);eventDraft.name=$("v15EventName").value;eventDraft.x=integer($("v15EventX").value);eventDraft.y=integer($("v15EventY").value);const report=validateCommands(currentPage().list);if(!report.ok)throw new Error(report.errors.map(item=>item.message).join(" "));await api.updateEvent(eventDraft,"Creator Console event",state.selectedMapId);const after=await api.mapSnapshot(state.selectedMapId);recordHistory({kind:"map",mapId:state.selectedMapId,before,after,label:`Edit ${eventDraft.name}`});eventDraftDirty=false;eventDraftMapId=state.selectedMapId;eventDraftEventId=eventDraft.id;state.onboarding.event=true;persist();notify("Event applied as a reversible authoring-layer change.","success");return renderEvents();}
        if(action==="event-new"){const before=await api.mapSnapshot(state.selectedMapId),id=Math.max(0,...(currentMap.events||[]).filter(Boolean).map(item=>item.id))+1;eventDraft={id,name:`Event ${id}`,note:"",x:0,y:0,pages:[newPage()]};await api.updateEvent(eventDraft,"Create event",state.selectedMapId);const after=await api.mapSnapshot(state.selectedMapId);recordHistory({kind:"map",mapId:state.selectedMapId,before,after,label:`Create ${eventDraft.name}`});state.selectedEventId=id;state.selectedPage=0;state.selectedCommand=0;eventDraftDirty=false;eventDraftMapId=state.selectedMapId;eventDraftEventId=id;state.onboarding.event=true;persist();return renderEvents();}
        if(action==="event-command-add"||action==="event-command-update"){const page=currentPage(),existing=page.list[state.selectedCommand],command=collectVisualCommand(existing);if(action.endsWith("add")){const at=Math.max(0,page.list.length-1);page.list.splice(at,0,command);state.selectedCommand=at;}else page.list[state.selectedCommand]=command;return renderEvents();}
        if(action==="event-param-add"){currentPage().list[state.selectedCommand].parameters.push("");return renderEvents();}if(action==="event-delete"){const page=currentPage();if(integer(page.list[state.selectedCommand]?.code)!==0)page.list.splice(state.selectedCommand,1);state.selectedCommand=clamp(state.selectedCommand,0,page.list.length-1);return renderEvents();}if(action==="event-duplicate"){const page=currentPage(),command=page.list[state.selectedCommand];if(integer(command?.code)!==0)page.list.splice(state.selectedCommand+1,0,clone(command));return renderEvents();}if(["event-up","event-down"].includes(action)){const page=currentPage(),to=clamp(state.selectedCommand+(action.endsWith("up")?-1:1),0,page.list.length-2);[page.list[state.selectedCommand],page.list[to]]=[page.list[to],page.list[state.selectedCommand]];state.selectedCommand=to;return renderEvents();}if(action==="event-indent"||action==="event-outdent"){const command=currentPage().list[state.selectedCommand];command.indent=Math.max(0,integer(command.indent)+(action==="event-indent"?1:-1));return renderEvents();}if(action==="event-page-add"){eventDraft.pages.push(newPage());state.selectedPage=eventDraft.pages.length-1;state.selectedCommand=0;return renderEvents();}
        if(action==="event-page-update"){const page=currentPage(),conditions=page.conditions||{};page.trigger=integer($("v15PageTrigger").value);page.priorityType=integer($("v15PagePriority").value);conditions.switch1Valid=$("v15CondSwitch").checked;conditions.switch1Id=integer($("v15CondSwitchId").value,1);conditions.variableValid=$("v15CondVariable").checked;conditions.variableId=integer($("v15CondVariableId").value,1);conditions.variableValue=integer($("v15CondVariableValue").value);conditions.selfSwitchValid=$("v15CondSelf").checked;conditions.selfSwitchCh=$("v15CondSelfValue").value;page.conditions=conditions;page.walkAnime=$("v15WalkAnime").checked;page.through=$("v15Through").checked;notify("Page settings updated in the draft.");return renderEvents();}
        if(action==="event-route-add"){const command=currentPage().list[state.selectedCommand],route=command.parameters[1]||{list:[{code:0,parameters:[]}],repeat:true,skippable:false,wait:false},entry={code:integer($("v15RouteCode").value),parameters:String($("v15RouteParams").value).split("|").filter(Boolean).map(value=>parseValue(value.trim()))};route.list ||= [{code:0,parameters:[]}];route.list.splice(Math.max(0,route.list.length-1),0,entry);command.parameters[1]=route;return renderEvents();}if(action==="event-plugin-refresh"){await loadPluginCommandMetadata();notify(`${pluginCommands.length} Plugin Manager commands discovered.`,`success`);return renderEvents();}
        if(action==="tileset-apply-flag"){const selected=tilesets.find(item=>item?.id===integer(state.selectedTilesetId));let flag=0;document.querySelectorAll("[data-v15-flag]").forEach(input=>{if(input.checked)flag|=integer(input.dataset.v15Flag);});flag|=clamp(integer($("v15Terrain").value),0,7)<<12;selected.flags[state.selectedTileId]=flag;tilesetsDirty=true;state.onboarding.tileset=true;persist();notify(`Tile ${state.selectedTileId} flag updated in the working copy.`);return renderTilesets();}
        if(action==="tileset-semantic"){const id=String(state.selectedTilesetId),profile=state.semanticProfiles[id]||{tilesetId:integer(id),labels:{}};profile.labels[String(state.selectedTileId)]=String($("v15SemanticLabels").value).split(",").map(value=>value.trim()).filter(Boolean);state.semanticProfiles[id]=profile;if(api.projectInfo().open)await transactionalWrite(".hybrid/semantic-tilesets.json",`${JSON.stringify(state.semanticProfiles,null,2)}\n`,"Semantic tiles");persist();notify("Semantic labels saved.","success");return renderTilesets();}
        if(action==="tileset-save"){const text=`${JSON.stringify(tilesets)}\n`;await transactionalWrite("data/Tilesets.json",text,"Tileset flags");tilesetsDirty=false;notify("Tilesets.json saved with a transaction backup.","success");return renderTilesets();}if(action==="tileset-audit")return notify(`${tilesetIssues(tilesets.find(item=>item?.id===integer(state.selectedTilesetId))).length} tileset findings detected.`);if(action==="tileset-raw-toggle"){const container=$("v15RawSheets");if(container.hidden)return loadRawSheets(tilesets.find(item=>item?.id===integer(state.selectedTilesetId)));container.hidden=true;return;}if(action==="tileset-backups")return showBackups("Tilesets.json");
        if(action==="live-connect")return startLive();if(action==="live-stop")return stopLive();if(action==="live-ping"){await sendLive("ping");return pollLive(true);}if(action==="live-watch"){state.live.watchedSwitches=$("v15WatchSwitches").value;state.live.watchedVariables=$("v15WatchVariables").value;await sendLive("startSession",{clientId:"studio-v15",protocolVersion:2,watchedSwitches:state.live.watchedSwitches.split(/[, ]+/).map(Number).filter(Boolean),watchedVariables:state.live.watchedVariables.split(/[, ]+/).map(Number).filter(Boolean)});persist();return notify("Watched state updated.","success");}if(action==="live-reload"){await sendLive("reloadRecipes");return notify("Recipe reload requested.");}if(action==="live-recovery"){await sendLive("recoveryPoint",{name:"Creator Console live restore point"});return notify("Runtime restore point requested.");}if(action==="live-pause"){await sendLive("pause");return notify("World systems pause requested.");}if(action==="live-resume"){await sendLive("resume");return notify("World systems resumed.");}if(action==="live-diagnostics"){await sendLive("diagnostics");return notify("Diagnostic snapshot requested.");}if(action==="live-clear-log"){state.live.logs=[];persist();return renderLive();}if(action==="live-import")return importLastRecording().then(()=>switchView("tests"));if(action==="record-start"){await sendLive("startRecording",{name:`Journey ${new Date().toLocaleString()}`});state.live.recording=true;persist();return renderLive();}if(action==="record-stop"){await sendLive("stopRecording");state.live.recording=false;persist();await delay(500);try{await importLastRecording();}catch(_){}return switchView("tests");}if(action==="live-clean"){if(!cleanupArmed){cleanupArmed=true;notify("Press cleanup again to remove only stale Live bridge files.");return renderLive();}cleanupArmed=false;for(const path of ["data/HybridTileLiveState.json","data/HybridTileLiveCommands.json","data/HybridTileLastRecording.json"])if(await api.projectEntryExists(path))await api.removeProjectEntry(path);notify("Live bridge artifacts removed.","success");return renderLive();}
        if(action==="test-import"||action==="team-import"||action==="extension-import"){$("v15Import").dataset.mode=action;$("v15Import").click();return;}if(action==="test-new"){const scenario={id:uid("scenario"),format:"HybridPlaytestScenario",version:2,studioVersion:"15.0.0",name:"New player journey",createdAt:Date.now(),setup:{mapId:api.state().activeMapId,player:{x:0,y:0,direction:2},switches:{},variables:{}},steps:[],assertions:{mapId:api.state().activeMapId,player:{x:0,y:0,direction:2},switches:{},variables:{}}};state.scenarios.unshift(scenario);state.selectedScenarioId=scenario.id;persist();return renderTests();}if(action==="test-step-add"){const scenario=selectedScenario();scenario.steps.push(parseStep($("v15NewStepType").value,$("v15NewStepValue").value));persist();return renderTests();}if(action==="test-state-update"){const scenario=selectedScenario();scenario.name=$("v15ScenarioName").value;for(const prefix of ["setup","assertions"]){scenario[prefix]={mapId:integer($(`v15-${prefix}-map`).value),player:{x:integer($(`v15-${prefix}-x`).value),y:integer($(`v15-${prefix}-y`).value),direction:2},switches:parsePairs($(`v15-${prefix}-switches`).value),variables:parsePairs($(`v15-${prefix}-variables`).value)};}persist();notify("Journey setup and assertions updated.");return renderTests();}if(action==="test-export"){const scenario=selectedScenario();api.download(`${safeId(scenario.name)}.htgscenario`,JSON.stringify(scenario,null,2));return;}if(action==="test-run"){const scenario=selectedScenario();let run;if(state.live.connected){await sendLive("runJourney",{scenario,options:{execute:true,continueOnFailure:true}});run={scenarioId:scenario.id,passed:true,failures:0,remote:true};}else{const errors=[];if(!scenario.steps.length)errors.push("Journey has no steps.");run={scenarioId:scenario.id,passed:!errors.length,failures:errors.length,errors,dryRun:true};}state.scenarioRuns.unshift(run);persist();notify(run.passed?"Journey accepted by the test runner.":"Journey needs attention.",run.passed?"success":"error");return renderTests();}if(action==="test-run-all"){const run=await runStudioSuite();notify(run.passed?"Complete suite passed.":`${run.failures} suite failures.`,run.passed?"success":"error");return renderTests();}if(action==="golden-capture"){await captureGoldenMaps();return renderTests();}if(action==="golden-compare"){const results=await compareGoldenMaps(),failed=results.filter(item=>!item.passed);notify(failed.length?`${failed.length} golden maps changed.`:"Every golden map matches.",failed.length?"error":"success");return renderTests();}if(action==="test-cli"){const manifest={format:"HybridHeadlessTestManifest",version:1,studioVersion:"15.0.0",project:api.projectInfo().name,scenarios:state.scenarios,goldenMaps:state.goldenMaps,command:"node HybridTileStudioCLI.js validate-project <project> --manifest <file>"};api.download("HybridTileStudio-Tests.htgtestmanifest",JSON.stringify(manifest,null,2));return;}
        if(action==="search-rename-preview")return buildRenamePlan();if(action==="search-rename-apply")return applyRenamePlan();if(action==="search-bookmarks")return notify(`${state.bookmarks.length} project bookmarks are shown in the rename panel.`);
        if(action==="intel-refresh")return renderIntelligence();if(action==="intel-doctor"){close();return v13.open("repair");}if(action==="intel-auto-label"){const profile=state.semanticProfiles[String(intelligence.map.tilesetId)]||{tilesetId:intelligence.map.tilesetId,labels:{}};for(const[item,index]of intelligence.semantic.frequent.entries())if(!profile.labels[item.tileId])profile.labels[item.tileId]=[index===0?"primary-floor":index<4?"common-surface":"world-detail"];state.semanticProfiles[String(intelligence.map.tilesetId)]=profile;persist();notify("Semantic suggestions created for the most frequent ground tiles.","success");return renderIntelligence();}
        if(action==="perf-current"){await buildPerformanceReport(false);return renderPerformance();}if(action==="perf-all"){await buildPerformanceReport(true);return renderPerformance();}if(action==="perf-export"){api.download("HybridTileStudio-Performance-v15.json",JSON.stringify(performanceReport,null,2));return;}if(action==="perf-legacy"){close();return v13.open("release");}if(action==="perf-compat"){await runCompatibility();notify("Compatibility profiles completed.","success");return renderPerformance();}
        if(action==="extension-probe"){const selected=state.extensionManifests.find(item=>item.id===state.selectedExtensionId)||state.extensionManifests[0];state.extensionProbe=await probeExtension(selected);const policy=state.extensionSecurity[selected.id]||{};if(!state.extensionProbe.ok){policy.failures=list(policy.failures);policy.failures.push({at:Date.now(),error:state.extensionProbe.error||state.extensionProbe.errors.join(" ")});if(policy.failures.length>=3)policy.quarantined=true;}state.extensionSecurity[selected.id]=policy;persist();notify(state.extensionProbe.ok?"Worker probe passed.":"Extension probe failed.",state.extensionProbe.ok?"success":"error");return renderExtensions();}if(action==="extension-policy"){const selected=state.extensionManifests.find(item=>item.id===state.selectedExtensionId)||state.extensionManifests[0];state.extensionSecurity[selected.id]=Object.assign({},state.extensionSecurity[selected.id],{isolation:$("v15ExtIsolation").value,timeBudgetMs:integer($("v15ExtTime").value),payloadBudgetKb:integer($("v15ExtPayload").value),network:$("v15ExtNetwork").checked,fileRead:$("v15ExtRead").value.split(",").map(value=>value.trim()).filter(Boolean),fileWrite:$("v15ExtWrite").value.split(",").map(value=>value.trim()).filter(Boolean),publisherId:$("v15PublisherId").value,updatedAt:Date.now()});await transactionalWrite(".hybrid/extension-security-v15.json",`${JSON.stringify(state.extensionSecurity,null,2)}\n`,"Extension security policy");persist();notify("Extension security policy saved.","success");return renderExtensions();}if(action==="extension-publisher"){const id=safeId($("v15PublisherId").value);if(!id)throw new Error("Enter a publisher ID.");state.publishers[id]={id,name:id,fingerprint:$("v15PublisherFingerprint").value,trusted:$("v15PublisherTrusted").checked,updatedAt:Date.now()};await transactionalWrite(".hybrid/extension-publishers-v15.json",`${JSON.stringify(state.publishers,null,2)}\n`,"Extension publishers");persist();notify("Local publisher record saved.","success");return renderExtensions();}if(action==="extension-quarantine"){const selected=state.extensionManifests.find(item=>item.id===state.selectedExtensionId)||state.extensionManifests[0],policy=state.extensionSecurity[selected.id]||{};policy.quarantined=!policy.quarantined;state.extensionSecurity[selected.id]=policy;persist();notify(policy.quarantined?"Extension quarantined.":"Extension quarantine cleared.",policy.quarantined?"error":"success");return renderExtensions();}if(action==="extension-backups")return showBackups("extension");if(action==="extension-compat")return showCompatibility();
        if(action==="team-git"){state.gitOutput=await api.gitStatus();persist();return renderTeam();}if(action==="team-diff"){const text=await api.git(["diff","--","data",".hybrid"]);state.gitDiff=String(text).split("\n").slice(0,500).map(line=>({kind:line.startsWith("+")?"add":line.startsWith("-")?"remove":"context",text:line}));persist();return renderTeam();}if(action==="team-bundle")return createReviewBundle().then(()=>renderTeam());if(action==="team-branch"){const branch=api.createBranch();notify(branch?"Visual branch created.":"Branch creation was cancelled.",branch?"success":"info");return renderTeam();}if(action==="team-reviews"){close();document.querySelector("#inspectorTabs [data-panel='diffPanel']")?.click();return;}if(action==="team-compare"){const bundles=[...state.reviewBundles,...state.importedBundles.filter(item=>item.format==="HybridCollaborationBundle")];state.bundleComparison=compareBundles(bundles[integer($("v15BundleA").value)],bundles[integer($("v15BundleB").value)]);persist();return renderTeam();}if(action==="team-merge-plan"){const plan={format:"HybridCollaborationMergePlan",version:1,studioVersion:"15.0.0",createdAt:new Date().toISOString(),comparison:state.bundleComparison,strategy:"manual",applySupported:false};api.download("HybridTileStudio-Merge-Plan.htgmerge",JSON.stringify(plan,null,2));return;}
        if(action==="ship-runtime"){close();return v13.open("release");}if(action==="ship-check"){const report=await runShipCheck();notify(report.ready?"Every release gate passed.":`${report.errors} blocking release errors remain.`,report.ready?"success":"error");return renderShip();}if(action==="ship-report"){api.download("HybridTileStudio-Release-Report-v15.json",JSON.stringify(state.releaseReports[0],null,2));return;}if(action==="ship-clean"){const report=state.releaseReports[0]||await runShipCheck(),bundle={format:"HybridCleanProductionBundle",version:2,studioVersion:"15.0.0",createdAt:new Date().toISOString(),project:api.projectInfo(),workspace:parseJson(api.canonicalWorkspaceText(),{}),release:{fingerprint:report.fingerprint,ready:report.ready},excluded:["data/HybridTileLiveState.json","data/HybridTileLiveCommands.json","data/HybridTileLastRecording.json",".hybrid/recovery/",".hybrid/reviews/","playtest recordings","scenario run history"]};api.download(`${safeId(api.projectInfo().name)}-production.htgrelease`,JSON.stringify(bundle,null,2));return notify("Clean production handoff exported.","success");}if(action==="ship-fingerprint"){const report=state.releaseReports[0]||await runShipCheck(),fingerprint={id:uid("fingerprint"),format:"HybridReleaseFingerprint",version:2,studioVersion:"15.0.0",project:api.projectInfo().name,channel:$("v15ReleaseChannel")?.value||"stable",createdAt:Date.now(),workspaceHash:api.checksum(parseJson(api.canonicalWorkspaceText(),{})),testsHash:api.checksum(state.scenarios),goldenHash:api.checksum(state.goldenMaps),extensionHash:api.checksum(state.extensionSecurity),fingerprint:report.fingerprint};state.releaseFingerprints.unshift(fingerprint);persist();api.download(`${safeId(api.projectInfo().name)}-${fingerprint.fingerprint}.htgfingerprint`,JSON.stringify(fingerprint,null,2));return renderShip();}if(action==="ship-compare"){const from=state.releaseFingerprints[integer($("v15FingerprintA").value)],to=state.releaseFingerprints[integer($("v15FingerprintB").value)],fields=["workspaceHash","testsHash","goldenHash","extensionHash","channel"];state.releaseComparison={from:from.id,to:to.id,changes:fields.filter(field=>from[field]!==to[field]).map(field=>({field,from:from[field],to:to[field]})),identical:from.fingerprint===to.fingerprint};persist();return renderShip();}if(action==="ship-manifest"){const report=state.releaseReports[0]||await runShipCheck(),manifest={format:"HybridReleaseManifest",version:1,studioVersion:"15.0.0",createdAt:new Date().toISOString(),channel:$("v15ReleaseChannel")?.value||"stable",fingerprint:report.fingerprint,targets:{windows:["nsis","portable","zip"],macos:["dmg","zip"],linux:["AppImage","tar.gz"]},signing:{windows:"WIN_CSC_LINK + password",macos:"Developer ID + notarization",linux:"optional"},updates:{channel:$("v15ReleaseChannel")?.value||"stable",metadata:"latest*.yml must match artifacts"},exclusions:report.bridgeFiles,workflow:".github/workflows/build-desktop.yml"};api.download("HybridTileStudio-Release-Manifest.htgmanifest",JSON.stringify(manifest,null,2));return;}if(action==="ship-ci")return exportCiTemplates();
    }

    async function openPractice(){const response=await fetch("examples/PracticeMap001.json"),text=await response.text(),file=new File([text],"Map001.json",{type:"application/json"});await api.openMapFiles([file]);state.selectedMapId=1;state.onboarding.project=true;persist();switchView("events");notify("Practice workspace opened. Changes remain local until exported.","success");}
    async function showBackups(filter=""){let entries=[];try{entries=await api.listProjectDirectory(".hybrid/backups");}catch(_){}$("v15SheetKicker").textContent="RECOVERY BROWSER";$("v15SheetTitle").textContent="Backups";$("v15SheetBody").innerHTML=entries.filter(item=>!filter||item.name.toLowerCase().includes(filter.toLowerCase())).map(item=>`<article><i>↶</i><span><strong>${escapeHtml(item.name)}</strong><small>${escapeHtml(item.path)}</small></span></article>`).join("")||"<p>No matching backups were found.</p>";$("v15Sheet").showModal();}
    async function showCompatibility(){const report=await runCompatibility();$("v15SheetKicker").textContent="PUBLIC-API COMPATIBILITY";$("v15SheetTitle").textContent="Compatibility profiles";$("v15SheetBody").innerHTML=report.results.map(item=>`<article><i>${item.detected.length?"✓":"•"}</i><span><strong>${escapeHtml(item.name)}</strong><small>${item.detected.length?`Detected: ${item.detected.join(", ")}`:"Not detected"}</small><p>${escapeHtml(item.guidance)}</p></span></article>`).join("");$("v15Sheet").showModal();}
    function exportCiTemplates(){const workflow=`name: Hybrid Tile Studio v15\non: [push, workflow_dispatch]\njobs:\n  validate:\n    runs-on: ubuntu-latest\n    steps:\n      - uses: actions/checkout@v4\n      - uses: actions/setup-node@v4\n        with: { node-version: 22 }\n      - run: npm ci\n      - run: node HybridTileStudioCLI.js validate-project . --manifest HybridTileStudio-Tests.htgtestmanifest\n      - run: npm test\n`;api.download("hybrid-tile-studio-v15.yml",workflow,"text/yaml");}
    async function importFiles(files){for(const file of files){const value=parseJson(await file.text(),null);if(!value)continue;importAssociated(value,file.name);}$("v15Import").value="";}
    async function importAssociated(value,name="file"){
        if(!value)return;
        if(value.format==="HybridPlaytestRecording"){state.recordings=state.recordings.filter(item=>item.id!==value.id);state.recordings.unshift(value);state.onboarding.test=true;notify(`Imported recording ${name}.`,`success`);if($("v15Studio")?.open)switchView("tests");}
        else if(["HybridPlaytestScenario","HybridPlaytestJourney"].includes(value.format)){state.scenarios=state.scenarios.filter(item=>item.id!==value.id);state.scenarios.unshift(value);state.selectedScenarioId=value.id;notify(`Imported journey ${name}.`,`success`);if($("v15Studio")?.open)switchView("tests");}
        else if(value.format==="HybridHeadlessTestManifest"){for(const scenario of list(value.scenarios)){state.scenarios=state.scenarios.filter(item=>item.id!==scenario.id);state.scenarios.push(scenario);}state.goldenMaps=Object.assign({},state.goldenMaps,value.goldenMaps||{});notify(`Imported ${list(value.scenarios).length} journeys and ${Object.keys(value.goldenMaps||{}).length} golden maps.`,`success`);if($("v15Studio")?.open)switchView("tests");}
        else if(value.format==="HybridCollaborationBundle"){state.importedBundles=state.importedBundles.filter(item=>item.id!==value.id);state.importedBundles.unshift(value);notify(`Imported review bundle ${name}.`,`success`);if($("v15Studio")?.open)switchView("team");}
        else if(value.format==="HybridCollaborationMergePlan"){state.importedMergePlan=value;notify(`Imported merge plan ${name} for inspection.`,`success`);if($("v15Studio")?.open)switchView("team");}
        else if(value.format==="HybridReleaseFingerprint"){state.releaseFingerprints=state.releaseFingerprints.filter(item=>item.id!==value.id);state.releaseFingerprints.unshift(value);notify(`Imported fingerprint ${name}.`,`success`);if($("v15Studio")?.open)switchView("ship");}
        else if(["HybridReleaseManifest","HybridCleanProductionBundle"].includes(value.format)){state.importedReleaseArtifact=value;notify(`Imported release artifact ${name} for inspection.`,`success`);if($("v15Studio")?.open)switchView("ship");}
        else if(value.format==="HybridUniversalRecoveryPoint"){state.recoveryPoints=state.recoveryPoints.filter(item=>item.id!==value.id);state.recoveryPoints.unshift(value);notify(`Imported recovery metadata ${name}. Backups remain project-local.`,`success`);if($("v15Studio")?.open)switchView("home");}
        else if(value.format==="HybridTileStudioExtension"){if(!api.projectInfo().open)throw new Error("Open a project before installing an extension manifest.");const id=safeId(value.id);let old=null;try{old=await api.readProjectText(`.hybrid/extensions/${id}.json`);}catch(_){}if(old!==null)await api.writeProjectText(`.hybrid/backups/extensions/${id}.${Date.now()}.json`,old);await api.writeProjectJson(`.hybrid/extensions/${id}.json`,value,true);state.selectedExtensionId=id;notify(`Installed extension manifest ${id}.`,`success`);if($("v15Studio")?.open)switchView("extensions");}
        else if(value.format==="HybridCompatibilityProfile"){state.compatibilityProfiles=state.compatibilityProfiles.filter(item=>item.id!==value.id);state.compatibilityProfiles.push(value);notify(`Imported compatibility profile ${name}.`,`success`);if($("v15Studio")?.open)switchView("performance");}
        else notify(`${name} is not a recognized Creator Console artifact.`,`error`);
        persist();
    }

    let commandItems=[];
    function buildCommands(){commandItems=[...VIEWS.map(([id,label])=>({id:`view:${id}`,label:`Open ${label}`,detail:"Creator Console area"})),{id:"project",label:"Open RPG Maker project",detail:"Project"},{id:"save",label:"Save all maps",detail:"Atomic save"},{id:"recovery",label:"Create universal restore point",detail:"Safety"},{id:"live",label:"Launch or attach playtest",detail:"Live Production 2.0"},{id:"record",label:"Start journey recording",detail:"Test Lab"},{id:"search",label:"Search project",detail:"References"},{id:"check",label:"Run every release gate",detail:"Ship"},...api.projectInfo().maps.map(item=>({id:`map:${item.id}`,label:`Open ${item.name}`,detail:`Map ${item.id}`}))];}
    function openPalette(){buildCommands();$("v15PaletteQuery").value="";renderPalette();$("v15Palette").showModal();setTimeout(()=>$("v15PaletteQuery").focus(),20);}
    function renderPalette(){const query=$("v15PaletteQuery").value.toLowerCase(),recent=new Set(state.recentCommands.slice(0,6));$("v15PaletteResults").innerHTML=commandItems.filter(item=>!query||`${item.label} ${item.detail}`.toLowerCase().includes(query)).sort((a,b)=>Number(recent.has(b.id))-Number(recent.has(a.id))).slice(0,60).map(item=>`<button type="button" data-v15-command="${escapeHtml(item.id)}"><i>${item.id.startsWith("map")?"▦":item.id.startsWith("view")?"◇":"→"}</i><span><strong>${escapeHtml(item.label)}</strong><small>${escapeHtml(item.detail)}</small></span>${recent.has(item.id)?"<b>RECENT</b>":""}</button>`).join("")||"<p>No matching commands.</p>";}
    async function executeCommand(id){state.recentCommands=[id,...state.recentCommands.filter(value=>value!==id)].slice(0,30);persist();if(id.startsWith("view:"))return switchView(id.slice(5));if(id.startsWith("map:")){await api.activateMap(integer(id.slice(4)));state.selectedMapId=integer(id.slice(4));return switchView("intelligence");}if(id==="project")return api.openProject();if(id==="save")return api.saveAll().then(()=>notify("All maps saved.","success"));if(id==="recovery")return createRecoveryPoint();if(id==="live"){switchView("live");return startLive();}if(id==="record"){switchView("live");return runAction("record-start");}if(id==="search")return switchView("search");if(id==="check"){switchView("ship");return runAction("ship-check");}}
    function focusables(){return[...$("v15Studio").querySelectorAll('button:not(:disabled),input:not(:disabled),select:not(:disabled),textarea:not(:disabled),[tabindex="0"]')].filter(item=>item.offsetParent!==null);}
    function spatialMove(direction){const items=focusables();if(!items.length)return;const current=items.includes(document.activeElement)?document.activeElement:items[0],rect=current.getBoundingClientRect(),center={x:rect.left+rect.width/2,y:rect.top+rect.height/2};const choices=items.filter(item=>item!==current).map(item=>{const box=item.getBoundingClientRect(),dx=box.left+box.width/2-center.x,dy=box.top+box.height/2-center.y,valid=direction==="left"?dx<-3:direction==="right"?dx>3:direction==="up"?dy<-3:dy>3,primary=/left|right/.test(direction)?Math.abs(dx):Math.abs(dy),cross=/left|right/.test(direction)?Math.abs(dy):Math.abs(dx);return{item,valid,score:primary+cross*2.4};}).filter(item=>item.valid).sort((a,b)=>a.score-b.score);(choices[0]?.item||current).focus();tone("move");}
    function cycleView(offset){const index=Math.max(0,VIEWS.findIndex(item=>item[0]===state.view));switchView(VIEWS[(index+offset+VIEWS.length)%VIEWS.length][0]);}
    function handleKeys(event){if(!$("v15Studio")?.open)return;if(/INPUT|TEXTAREA|SELECT/.test(event.target.tagName)&&event.key!=="Escape"&&!((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="k"))return;if(["ArrowLeft","ArrowRight","ArrowUp","ArrowDown"].includes(event.key)){event.preventDefault();event.stopImmediatePropagation();spatialMove(event.key.replace("Arrow","").toLowerCase());}else if(event.key==="Escape"){event.preventDefault();event.stopImmediatePropagation();const nested=[...document.querySelectorAll("dialog[open]")].filter(item=>item!==$("v15Studio")).at(-1);if(nested)nested.close();else close();}else if(event.key.toLowerCase()==="x"||((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="k")){event.preventDefault();event.stopImmediatePropagation();openPalette();}else if(event.key.toLowerCase()==="y"||event.key==="?"){event.preventDefault();event.stopImmediatePropagation();openGuide();}else if((event.ctrlKey||event.metaKey)&&event.key.toLowerCase()==="z"){event.preventDefault();event.stopImmediatePropagation();event.shiftKey?redoUniversal():undoUniversal();}else if(event.key==="PageUp"){event.preventDefault();event.stopImmediatePropagation();cycleView(-1);}else if(event.key==="PageDown"){event.preventDefault();event.stopImmediatePropagation();cycleView(1);}}
    function pollGamepad(){if($("v15Studio")?.open){const pad=navigator.getGamepads?.()[0];if(pad){const pressed=index=>!!pad.buttons[index]?.pressed,edge=(name,value,callback)=>{if(value&&!lastGamepad[name])callback();lastGamepad[name]=value;};edge("a",pressed(0),()=>document.activeElement?.click?.());edge("b",pressed(1),close);edge("x",pressed(2),openPalette);edge("y",pressed(3),openGuide);edge("up",pressed(12)||pad.axes[1]<-.65,()=>spatialMove("up"));edge("down",pressed(13)||pad.axes[1]>.65,()=>spatialMove("down"));edge("left",pressed(14)||pad.axes[0]<-.65,()=>spatialMove("left"));edge("right",pressed(15)||pad.axes[0]>.65,()=>spatialMove("right"));edge("lb",pressed(4),()=>cycleView(-1));edge("rb",pressed(5),()=>cycleView(1));}}requestAnimationFrame(pollGamepad);}

    window.HybridTileStudioV15=Object.freeze({version:"15.0.0",open,close,switchView,validateCommands,searchProject,analyzeIntelligence,buildPerformanceReport,probeExtension,runCompatibility,runShipCheck,createRecoveryPoint,state:()=>clone(state)});
    inject();
})();
